---
phase: 03.2-neon-postgres-migration-vercel-deployment
plan: 03
subsystem: infra
tags: [vercel, neon, postgres, prisma, cron, search, deployment]

# Dependency graph
requires:
  - phase: 03.2-02
    provides: Neon Postgres database provisioned, env vars set in Vercel, schema migrated

provides:
  - Daily cron jobs at 05:00 UTC (fetch) and 06:00 UTC (enrich) registered in Vercel
  - Case-insensitive article/source search via Postgres mode insensitive
  - All 6 sources seeded into Neon Postgres
  - Live Vercel production deployment with Postgres backend

affects: [phase-04-chat, future-cron-changes, search-improvements]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "tsx --env-file flag for loading .env.local in seed scripts without dotenv package"
    - "vercel env add via stdin pipe for non-interactive secret provisioning"

key-files:
  created: []
  modified:
    - vercel.json
    - src/app/api/articles/route.ts

key-decisions:
  - "Cron schedule: fetch at 05:00 UTC, enrich at 06:00 UTC (1-hour gap for fetch to complete before enrich)"
  - "mode: insensitive on Postgres contains replaces case-sensitive SQLite workaround; both title and source.name filters updated"
  - "CRON_SECRET piped from .env.local to vercel env add for non-interactive provisioning"

patterns-established:
  - "tsx --env-file=.env.local for running scripts with local env without dotenv dependency"

requirements-completed: [D-14, D-15, D-16, D-17, D-19]

# Metrics
duration: 5min
completed: 2026-03-21
---

# Phase 03.2 Plan 03: Deployment and Final Configuration Summary

**Vercel production deployment live with Neon Postgres, daily crons at 05:00/06:00 UTC, and case-insensitive search via Postgres mode insensitive**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-21T13:49:00Z
- **Completed:** 2026-03-21T13:54:37Z
- **Tasks:** 3 (2 auto + 1 auto-approved checkpoint)
- **Files modified:** 2

## Accomplishments

- Updated vercel.json cron schedule: fetch 04:00 -> 05:00 UTC, enrich 05:00 -> 06:00 UTC
- Added `mode: 'insensitive'` to both `title` and `source.name` contains filters in articles API (Postgres supports this, SQLite did not)
- Seeded 6 sources into Neon Postgres via `tsx --env-file` pattern
- Added `CRON_SECRET` to Vercel Production environment
- Deployed to production: `https://news-reader-eta.vercel.app`
- Build log confirmed: `prisma migrate deploy` (no pending migrations), `next build` compiled successfully

## Task Commits

Each task was committed atomically:

1. **Task 1: Update cron schedule, case-insensitive search, seed Postgres** - `be8a82c` (feat)
2. **Task 2: Deploy to Vercel and verify CRON_SECRET** - infrastructure only, no source file changes
3. **Task 3: Verify deployed application** - auto-approved checkpoint (auto_advance: true)

## Files Created/Modified

- `vercel.json` - Cron schedules updated to 05:00/06:00 UTC
- `src/app/api/articles/route.ts` - Added `mode: 'insensitive'` to search contains filters

## Decisions Made

- `tsx --env-file=.env.local` used to run seed script with local Neon credentials without requiring dotenv package
- CRON_SECRET piped from local env to `vercel env add` for non-interactive provisioning
- Task 2 had no source file commit since it was purely env + deploy infrastructure actions

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used tsx --env-file instead of npm run seed for Postgres seed**
- **Found during:** Task 1 (seed step)
- **Issue:** `npm run seed` does not load `.env.local`, causing `DATABASE_URL` not found error in tsx
- **Fix:** Ran `npx tsx --env-file=/path/.env.local prisma/seed.ts` directly
- **Files modified:** None (fix was invocation change only)
- **Verification:** Seed output showed all 6 sources with checkmarks, exit 0
- **Committed in:** be8a82c (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking/invocation)
**Impact on plan:** Seed ran correctly against Neon Postgres. No scope creep.

## Issues Encountered

- `npm run build` locally fails because `prisma migrate deploy` requires `DATABASE_URL_UNPOOLED` which is only in `.env.local`. Verified build by running `npx next build` directly (after `prisma generate`). Build compiles cleanly on Vercel where env vars are set.

## User Setup Required

None - CRON_SECRET added to Vercel automatically. Deployment complete.

## Known Stubs

None - all data wired. Sources seeded, search active, crons registered.

## Next Phase Readiness

- Phase 03.2 complete: application runs fully on Neon Postgres via Vercel
- Cron jobs registered and will run daily
- Case-insensitive search active on deployed instance
- Ready for Phase 04 (Chat interface)

---
*Phase: 03.2-neon-postgres-migration-vercel-deployment*
*Completed: 2026-03-21*
