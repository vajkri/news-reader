---
phase: 04.3-non-rss-source-ingestion-via-sitemap-parsing
verified: 2026-03-25T20:28:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
---

# Phase 04.3: Non-RSS Source Ingestion via Sitemap Parsing Verification Report

**Phase Goal:** Add sitemap-based source ingestion with strategy pattern dispatch, enabling Anthropic News and Claude Blog as non-RSS sources
**Verified:** 2026-03-25T20:28:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | `fetchSitemap` filters URLs by path pattern and 7-day cutoff | VERIFIED | `sitemap.ts` lines 42-54: substring filter on `pathPattern`, cutoff applied to `lastmod` before fetching |
| 2  | `extractArticleMeta` returns `ParsedArticle` with og:title, og:description, og:image, and datePublished | VERIFIED | `fetch-article-meta.ts` lines 23-72: extracts all four fields via cheerio selectors and JSON-LD regex |
| 3  | `extractArticleMeta` returns null on HTTP error or timeout | VERIFIED | `fetch-article-meta.ts` lines 19 and 73-75: `if (!res.ok) return null` + catch-all returning null |
| 4  | `fetchSitemap` handles sitemaps with and without lastmod dates | VERIFIED | `sitemap.ts` lines 48-79: splits into `withLastmod`/`withoutLastmod` branches with separate handling |
| 5  | `fetchArticles` dispatcher routes to correct strategy by sourceType string | VERIFIED | `fetch-strategies.ts` lines 19-31: `strategies` Record with `rss`, `sitemap`, `scrape` keys and `?? strategies.rss` fallback |
| 6  | Scrape strategy is scaffolded with correct signature returning empty array | VERIFIED | `scrape.ts` lines 10-18: correct `(listingUrl, linkSelector)` signature, `return []` |
| 7  | Source model has sourceType, sitemapPathPattern, scrapeUrl, scrapeLinkSelector fields | VERIFIED | `prisma/schema.prisma` lines 17-20: all four fields present, `@default("rss")` on sourceType |
| 8  | Anthropic News and Claude Blog exist as sitemap sources in the database after seeding | VERIFIED | `prisma/seed.ts` lines 14-15: both entries with `sourceType: "sitemap"` and correct `sitemapPathPattern` values |
| 9  | Fetch cron dispatches to correct strategy based on source.sourceType | VERIFIED | `src/app/api/fetch/route.ts` line 33: `fetchArticles(source)` replaces old `fetchFeed(source.url)` |
| 10 | Existing RSS sources continue to work unchanged after cron refactor | VERIFIED | `fetch-strategies.ts` line 20: `rss` key calls `fetchFeed(source.url)`; schema `@default("rss")` preserves existing rows |
| 11 | Only articles from the last 7 days are ingested for both RSS and sitemap sources | VERIFIED | `sitemap.ts` lines 52-74: 7-day cutoff enforced; `rss.ts` has `MAX_AGE_MS` (unchanged, verified by test suite passing) |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/fetch-article-meta.ts` | Shared HTML meta extraction helper | VERIFIED | 77 lines, exports `extractArticleMeta`, `import "server-only"`, uses cheerio |
| `src/lib/sitemap.ts` | Sitemap XML parsing and article discovery | VERIFIED | 83 lines, exports `fetchSitemap`, `import "server-only"`, uses XMLParser |
| `src/lib/scrape.ts` | Scrape strategy scaffold | VERIFIED | 18 lines, exports `fetchScrape`, `import "server-only"`, intentional stub |
| `src/lib/fetch-strategies.ts` | Strategy pattern dispatcher | VERIFIED | 33 lines, exports `fetchArticles` and `FetchSource` type, `import "server-only"` |
| `src/lib/fetch-article-meta.test.ts` | Unit tests for meta extraction | VERIFIED | 8 test cases covering og: extraction, JSON-LD, fallbacks, null cases, guid |
| `src/lib/sitemap.test.ts` | Unit tests for sitemap strategy | VERIFIED | 6 test cases covering path filtering, cutoff, no-lastmod, single-url, empty sitemap, old-date filtering |
| `src/lib/fetch-strategies.test.ts` | Unit tests for dispatcher | VERIFIED | 6 test cases covering all routes, default fallback, null pathPattern, scrapeUrl fallback |
| `prisma/schema.prisma` | Source model with sourceType and config fields | VERIFIED | Lines 17-20: all 4 fields present with correct types and defaults |
| `prisma/seed.ts` | Anthropic News and Claude Blog source entries | VERIFIED | Lines 14-15: both entries with `sourceType: "sitemap"` |
| `src/app/api/fetch/route.ts` | Strategy-dispatched fetch cron | VERIFIED | Imports `fetchArticles`, `findMany` selects all strategy fields, calls `fetchArticles(source)` |
| `prisma/migrations/20260325200000_add_source_type_fields/migration.sql` | Migration for new Source fields | VERIFIED | 4 ALTER TABLE ADD COLUMN statements, applied |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/sitemap.ts` | `src/lib/fetch-article-meta.ts` | `import extractArticleMeta` | WIRED | Line 3: `import { extractArticleMeta } from "./fetch-article-meta"` |
| `src/lib/fetch-strategies.ts` | `src/lib/rss.ts` | `import fetchFeed` | WIRED | Line 2: `import { fetchFeed } from "./rss"` |
| `src/lib/fetch-strategies.ts` | `src/lib/sitemap.ts` | `import fetchSitemap` | WIRED | Line 3: `import { fetchSitemap } from "./sitemap"` |
| `src/lib/sitemap.ts` | `src/lib/rss.ts` | `import ParsedArticle type` | WIRED | Line 4: `import type { ParsedArticle } from "./rss"` |
| `src/app/api/fetch/route.ts` | `src/lib/fetch-strategies.ts` | `import fetchArticles` | WIRED | Line 3: `import { fetchArticles } from "@/lib/fetch-strategies"` |
| `src/app/api/fetch/route.ts` | `prisma.source.findMany` | select includes sourceType and config fields | WIRED | Lines 13-22: select clause includes `sourceType`, `sitemapPathPattern`, `scrapeUrl`, `scrapeLinkSelector` |
| `prisma/seed.ts` | `prisma/schema.prisma` | source entries use sourceType field | WIRED | Lines 14-15: `sourceType: "sitemap"` matches schema field |

All key links WIRED.

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `src/app/api/fetch/route.ts` | `sources` (DB) | `prisma.source.findMany({ select: {...} })` | Yes — real DB query returning all sources including sitemap sources | FLOWING |
| `src/app/api/fetch/route.ts` | `articles` | `fetchArticles(source)` dispatches to `fetchSitemap` or `fetchFeed` | Yes — sitemap strategy fetches live sitemap XML and article pages | FLOWING |
| `src/lib/sitemap.ts` | `urls` | `fetch(sitemapUrl)` + XMLParser | Yes — live sitemap XML parsed into URL array | FLOWING |
| `src/lib/fetch-article-meta.ts` | `title/description/thumbnail/publishedAt` | `fetch(url)` + cheerio | Yes — live HTML page scraped for og: and JSON-LD meta | FLOWING |

Note: `scrape.ts` intentionally returns `[]` — this is a documented scaffold, not a data-flow gap.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 66 tests pass | `pnpm test` | 8 test files, 66 tests, 0 failures | PASS |
| `extractArticleMeta` exported | `grep -c "export async function extractArticleMeta" src/lib/fetch-article-meta.ts` | 1 | PASS |
| `fetchSitemap` exported | `grep -c "export async function fetchSitemap" src/lib/sitemap.ts` | 1 | PASS |
| `fetchArticles` exported | `grep -c "export async function fetchArticles" src/lib/fetch-strategies.ts` | 1 | PASS |
| Route uses strategy dispatcher | `grep "fetchArticles" src/app/api/fetch/route.ts` | match found | PASS |
| Route has no direct fetchFeed call | grep for fetchFeed in route.ts | 0 matches | PASS |
| Migration file exists | `prisma/migrations/20260325200000_add_source_type_fields/migration.sql` | File present, 4 ALTER TABLE statements | PASS |
| Dependencies in package.json | `grep '"fast-xml-parser"\|"cheerio"' package.json` | Both present at declared versions | PASS |

---

### Requirements Coverage

All requirement IDs declared in both plans cross-referenced against REQUIREMENTS.md:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SITE-01 | 04.3-01 | Strategy pattern dispatcher routes fetching by source type (rss, sitemap, scrape) | SATISFIED | `fetch-strategies.ts`: strategies Record + fetchArticles dispatcher; 6 tests pass |
| SITE-02 | 04.3-01 | Sitemap strategy parses XML, filters by path pattern and 7-day cutoff, returns ParsedArticle[] | SATISFIED | `sitemap.ts`: XMLParser + path filter + cutoff split + Promise.allSettled; 6 tests pass |
| SITE-03 | 04.3-01 | Article meta extraction from HTML pages (og:title, og:description, og:image, datePublished) | SATISFIED | `fetch-article-meta.ts`: cheerio extraction + JSON-LD/meta/lastmod fallback chain; 8 tests pass |
| SITE-04 | 04.3-02 | Source model extended with sourceType, sitemapPathPattern, scrapeUrl, scrapeLinkSelector | SATISFIED | schema.prisma lines 17-20 + migration SQL applied |
| SITE-05 | 04.3-02 | Anthropic News and Claude Blog added as sitemap sources | SATISFIED | seed.ts lines 14-15; both rows confirmed inserted via Neon HTTP in SUMMARY |
| SITE-06 | 04.3-02 | Fetch cron dispatches to correct strategy based on source.sourceType | SATISFIED | route.ts: imports fetchArticles, select includes sourceType, calls fetchArticles(source) |

All 6 requirements satisfied. No orphaned requirements found.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/lib/scrape.ts` | 16-17 | `console.warn` + `return []` | Info | Intentional documented scaffold — JSDoc marks it "Not yet implemented", not a regression |

No blocker or warning anti-patterns. The scrape stub is by design per plan spec (SITE-01 scope excludes scrape implementation).

---

### Human Verification Required

**1. Live Sitemap Fetch — Anthropic News**

**Test:** Trigger the fetch cron POST `/api/fetch` with a valid `CRON_SECRET` in production or staging. Check that articles from `anthropic.com/news/*` appear in the DB within seconds.
**Expected:** New Article rows with `sourceId` matching Anthropic News and `guid` values starting with `https://www.anthropic.com/news/`.
**Why human:** Requires live network access to anthropic.com sitemap and article pages, and a running database connection.

**2. Live Sitemap Fetch — Claude Blog**

**Test:** Same trigger as above. Check that articles from `claude.com/blog/*` appear in the DB.
**Expected:** New Article rows with `sourceId` matching Claude Blog and `guid` values starting with `https://claude.com/blog/`.
**Why human:** Same reasons — live network + DB access required.

---

### Gaps Summary

No gaps. All 11 observable truths verified, all 7 artifacts at all four levels (exist, substantive, wired, data flowing), all 7 key links wired, all 6 requirements satisfied, 66/66 tests passing.

---

_Verified: 2026-03-25T20:28:00Z_
_Verifier: Claude (gsd-verifier)_
