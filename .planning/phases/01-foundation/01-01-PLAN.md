---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - src/app/globals.css
  - src/app/api/fetch/route.ts
  - src/components/ui/input.tsx
  - .claude/memory/MEMORY.md
  - .claude/memory/feedback_no_raw_buttons.md
  - .claude/memory/feedback_section_container.md
  - .serena/project.yml
autonomous: true
requirements:
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04
  - FOUND-03

must_haves:
  truths:
    - "Claude Code opens the project and immediately has context on conventions, rules, and preferred patterns via CLAUDE.md"
    - "CLAUDE.md documents the actual design system of this project (zinc palette, Geist fonts, shadcn/ui components), not a different project"
    - "The cron endpoint rejects requests without a valid CRON_SECRET header and returns 401"
    - "The .section-container CSS utility exists in globals.css for consistent horizontal padding"
    - "The Input component uses focus-visible instead of bare focus for ring styles"
    - "Serena MCP is configured with project.yml pointing to the project root and the serena start script is documented"
    - "CLAUDE.md Tool Preferences documents available skills (Vercel Cron, investigation mode) and Serena MCP usage"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project conventions, design system, component organization, git rules, tool preferences with skills and Serena"
      contains: "Geist Sans"
    - path: "src/app/globals.css"
      provides: ".section-container utility class"
      contains: ".section-container"
    - path: "src/app/api/fetch/route.ts"
      provides: "Cron auth guard"
      contains: "CRON_SECRET"
    - path: "src/components/ui/input.tsx"
      provides: "Fixed focus ring using focus-visible"
      contains: "focus-visible"
    - path: ".serena/project.yml"
      provides: "Serena MCP project configuration"
      contains: "NewsReader"
  key_links:
    - from: "src/app/api/fetch/route.ts"
      to: "process.env.CRON_SECRET"
      via: "Authorization header check"
      pattern: "Bearer.*CRON_SECRET"
    - from: "CLAUDE.md"
      to: ".serena/project.yml"
      via: "Tool Preferences documents Serena MCP integration"
      pattern: "Serena"
---

<objective>
Set up the development environment conventions, verify Serena MCP and skills configuration, and fix the cron security gap.

Purpose: Establish the CLAUDE.md project conventions (so all subsequent plans follow the right patterns), verify Serena MCP integration is properly configured (DEV-04), document available skills for the project workflow (DEV-03), add the .section-container CSS utility (prerequisite for layout work in Plan 02), secure the cron endpoint with CRON_SECRET auth, fix the Input focus-visible bug, and clean up .claude/ memory files.

Output: Rewritten CLAUDE.md (with skills and Serena documentation), verified Serena config, secured cron endpoint, .section-container utility in globals.css, fixed Input component, cleaned memory files.
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Existing Input component signature (to be modified for focus-visible fix) -->
From src/components/ui/input.tsx:
```typescript
export const Input = React.forwardRef<
  HTMLInputElement,
  React.InputHTMLAttributes<HTMLInputElement>
>(({ className, ...props }, ref) => (
  <input
    ref={ref}
    className={cn(
      "flex h-9 w-full rounded-md border border-[var(--border)] bg-transparent px-3 py-1 text-sm shadow-sm transition-colors placeholder:text-[var(--muted-foreground)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)] disabled:cursor-not-allowed disabled:opacity-50",
      className
    )}
    {...props}
  />
));
```

From src/app/api/fetch/route.ts:
```typescript
export async function POST() {
  // No auth check currently — this is the security gap
  const sources = await prisma.source.findMany();
  // ...
}
```

From src/app/globals.css (current — no .section-container):
```css
:root {
  --background: #ffffff;
  --foreground: #0f0f0f;
  --muted: #f4f4f5;
  /* ... */
}
```

From .serena/project.yml (already configured):
```yaml
project_name: "NewsReader"
languages:
- typescript
- bash
encoding: "utf-8"
initial_prompt: "Always prefer Serena tools for code navigation and editing..."
```

From package.json (Serena start script already present):
```json
"serena": "uvx --from git+https://github.com/oraios/serena serena start-mcp-server --transport streamable-http --port 56667 --context ide --project . --open-web-dashboard false"
```

From .claude/settings.json (Serena MCP already wired):
```json
"serena": {
  "type": "http",
  "url": "http://localhost:56667/mcp"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md for this project and clean up .claude/ memory files</name>
  <files>CLAUDE.md, .claude/memory/MEMORY.md, .claude/memory/feedback_no_raw_buttons.md, .claude/memory/feedback_section_container.md</files>
  <read_first>
    - CLAUDE.md (current file — contains Lego project conventions that must be replaced)
    - .planning/phases/01-foundation/01-CONTEXT.md (locked decisions for CLAUDE.md content)
    - .planning/phases/01-foundation/01-UI-SPEC.md (design system specs: typography, color, spacing)
    - .planning/phases/01-foundation/01-RESEARCH.md (standard stack, architecture patterns)
    - src/app/globals.css (actual CSS tokens to document)
    - src/components/ui/button.tsx (actual Button variants to document)
    - src/components/ui/input.tsx (actual Input API to document)
    - src/components/ui/select.tsx (actual Select API to document)
    - src/components/ui/badge.tsx (actual Badge variants to document)
    - src/app/layout.tsx (font loading to document)
    - .claude/memory/MEMORY.md (index to update)
    - .claude/memory/feedback_no_raw_buttons.md (Button variants to correct)
    - .claude/memory/feedback_section_container.md (adapt for this project)
    - .claude/rules/memory-placement.md (placement rules to follow)
    - .claude/rules/no-em-dashes.md (existing rule to preserve)
  </read_first>
  <action>
Rewrite CLAUDE.md completely for this project (the AI News Tracker Companion). Keep the same structural template sections but replace ALL Lego-project-specific content. The new CLAUDE.md must contain these sections:

**## Component Organization**
- Folder Organization: `src/components/ui/` (primitives), `src/components/layout/` (layout), `src/components/features/` (feature components nested by page)
- Naming conventions: Single export = `PascalCase/`, Multiple exports = `lowercase/`
- Feature components: page-scoped in `features/{page}/`, standalone in `features/FeatureName/`
- Barrel exports: every component directory gets an `index.ts`

**## Tool Preferences**
- Code searches: Serena tools first, then glob, grep, shell
- Third-party docs: context7 MCP (resolve-library-id then query-docs)

**## Coding Conventions**
- Prefer Server Components by default; `'use client'` only when interactivity required
- Strict TypeScript: no `any` types, explicit return types on exports
- Conventional Commits format: feat:, fix:, docs:, chore:

**## Design System** (document the ACTUAL tokens from globals.css and UI-SPEC.md)
- Design Tokens: zinc palette CSS variables (--background, --foreground, --muted, --muted-foreground, --border, --primary, --primary-foreground, --secondary, --accent, --card, --radius), light/dark via prefers-color-scheme. Container tokens: --container-width (100dvw), --container-max-width (1800px), --container-padding (1.5rem). Used by `.section-container` utility.
- Font utilities: Geist Sans (`--font-geist-sans`) for UI, Geist Mono (`--font-geist-mono`) for code
- Typography scale: Body 14px/400, UI Label 14px/600, Heading 16px/600, Mono 13px/400. Min font size 13px. Badge exception at 12px.
- UI Components inventory: Button (4 variants: default/outline/ghost/destructive, sizes: default/sm/lg/icon), Input (h-9, focus-visible ring), Select (native with ChevronDown icon), Badge (3 variants: default/secondary/outline)
- Color: 60/30/10 rule. Dominant=--background, Secondary=--muted, Accent=--primary (reserved for CTA buttons, active filter tabs, focus rings, active nav)
- Search match highlight: `bg-yellow-200 dark:bg-yellow-800` on `<mark>` spans (functional, not brand accent)

**## Git**
- Never append `Co-Authored-By` lines to commit messages
- Conventional Commits format

**## Documentation & Memory**
- Same as current: CLAUDE.md for conventions, .claude/memory/ for behavioral, .planning/ for project state, .serena/memories/ for descriptive

**## Known Limitations**
- SQLite `contains` is case-sensitive (no `mode: 'insensitive'` support)
- `.env.local` must include `CRON_SECRET=any-dev-value` for local testing

**Memory file updates:**
1. `feedback_no_raw_buttons.md`: Change Button variant list from "yellow/red/grey/ghost" to "default/outline/ghost/destructive"
2. `feedback_section_container.md`: Update container token values to match this project (--container-max-width: 1800px, --container-padding: 1.5rem), remove Lego-specific references
3. `MEMORY.md`: Remove the stale `feedback_copy_holistic.md` entry if present. Verify all listed files actually exist.
  </action>
  <verify>
    <automated>grep -q "Geist Sans" CLAUDE.md && grep -q "section-container" CLAUDE.md && grep -q "default/outline/ghost/destructive" CLAUDE.md && grep -q "CRON_SECRET" CLAUDE.md && grep -q "default/outline/ghost/destructive" .claude/memory/feedback_no_raw_buttons.md && grep -q "1800px" .claude/memory/feedback_section_container.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "Geist Sans" (not "Baloo 2" or "DM Sans")
    - CLAUDE.md contains "section-container"
    - CLAUDE.md contains "default/outline/ghost/destructive" (actual Button variants)
    - CLAUDE.md does NOT contain "lego" or "Lego" or "Baloo" or "SlideFrame" or "StopBadge"
    - CLAUDE.md contains "CRON_SECRET" in Known Limitations
    - CLAUDE.md contains "Conventional Commits"
    - CLAUDE.md contains "Server Components" preference
    - feedback_no_raw_buttons.md contains "default/outline/ghost/destructive" (not "yellow/red/grey/ghost")
    - feedback_section_container.md contains "1800px" (not Lego values)
    - MEMORY.md does NOT contain "feedback_copy_holistic.md"
  </acceptance_criteria>
  <done>CLAUDE.md is fully rewritten for this project's actual tech stack and conventions. Memory files are corrected. DEV-01 and DEV-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Serena MCP config, document skills, and update CLAUDE.md Tool Preferences</name>
  <files>CLAUDE.md, .serena/project.yml</files>
  <read_first>
    - .serena/project.yml (Serena project config -- verify it is correct)
    - .serena/memories/ (list existing Serena memories to confirm onboarding was done)
    - .claude/settings.json (MCP server config -- verify Serena entry exists)
    - package.json (verify "serena" script exists)
    - CLAUDE.md (just written by Task 1 -- need to append to Tool Preferences section)
  </read_first>
  <action>
**A) Verify Serena MCP integration (DEV-04)**

Check that all three pieces of the Serena integration are in place:

1. `.serena/project.yml` exists with `project_name: "NewsReader"`, `languages: [typescript, bash]`, and `initial_prompt` referencing Serena tools. If correct, no changes needed. If missing or misconfigured, fix it.

2. `.claude/settings.json` contains the `serena` MCP server entry pointing to `http://localhost:56667/mcp`. Verify only -- this file should already be correct.

3. `package.json` contains a `"serena"` script. Verify only -- this should already be present.

If all three are verified, no file changes needed for .serena/project.yml. If any are missing, create/fix them.

**B) Document skills relevant to this project (DEV-03)**

Append to the **## Tool Preferences** section in CLAUDE.md (after the context7 MCP paragraph) a new subsection:

```markdown
### Available Skills

Skills are injected via the Vercel plugin bootstrap and provide domain expertise:

- **investigation-mode**: Orchestrated debugging for stuck/broken/hung states. Follow the triage order: runtime logs, workflow status, browser verification, deploy status. Always report evidence at every step.
- **cron-jobs**: Vercel Cron configuration in `vercel.json`. Cron handlers must verify `CRON_SECRET` via Authorization header. Hobby plan: max 2 jobs, min daily interval.

### Serena MCP

Serena provides semantic code intelligence (symbol search, reference finding, symbolic editing). Already configured:
- Config: `.serena/project.yml` (project_name: NewsReader, languages: typescript + bash)
- Start: `npm run serena` (starts MCP server on port 56667)
- MCP entry: `.claude/settings.json` maps `serena` to `http://localhost:56667/mcp`
- Memories: `.serena/memories/` (onboarding completed, project structure indexed)
```

**C) Update CLAUDE.md to document the Serena start script in a visible place**

In the **## Known Limitations** or **## Tool Preferences** section, ensure the `npm run serena` command is documented so future Claude sessions know how to verify the Serena server is running.
  </action>
  <verify>
    <automated>grep -q "investigation-mode" CLAUDE.md && grep -q "cron-jobs" CLAUDE.md && grep -q "Serena MCP" CLAUDE.md && grep -q "npm run serena" CLAUDE.md && grep -q "project_name" .serena/project.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "investigation-mode" skill documentation
    - CLAUDE.md contains "cron-jobs" skill documentation
    - CLAUDE.md contains "Serena MCP" subsection with config details
    - CLAUDE.md contains "npm run serena" start command
    - .serena/project.yml exists with project_name: "NewsReader"
    - .serena/project.yml contains languages: typescript, bash
    - .claude/settings.json contains serena MCP server entry (verified, not modified)
    - package.json contains "serena" script (verified, not modified)
  </acceptance_criteria>
  <done>Serena MCP integration verified and documented in CLAUDE.md (DEV-04). Available skills (investigation-mode, cron-jobs) documented in CLAUDE.md Tool Preferences (DEV-03).</done>
</task>

<task type="auto">
  <name>Task 3: Add .section-container to globals.css and fix Input focus-visible</name>
  <files>src/app/globals.css, src/components/ui/input.tsx</files>
  <read_first>
    - src/app/globals.css (current state — need to add container tokens and .section-container)
    - src/components/ui/input.tsx (current state — need to change focus: to focus-visible:)
    - .planning/phases/01-foundation/01-CONTEXT.md (locked .section-container CSS values)
    - .planning/phases/01-foundation/01-UI-SPEC.md (spacing scale, container utility spec)
  </read_first>
  <action>
**globals.css changes:**

Add container CSS custom properties to the `:root` block (append after existing variables):
```css
--container-width: 100dvw;
--container-max-width: 1800px;
--container-padding: 1.5rem;
```

Add `.section-container` utility class after the closing of the `@media (prefers-color-scheme: dark)` block and before the `*` rule:
```css
@layer components {
  .section-container {
    width: var(--container-width);
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
  }
}
```

**Input component fix:**

In `src/components/ui/input.tsx`, replace the two `focus:` class references with `focus-visible:`:
- Change `focus:outline-none` to `focus-visible:outline-none`
- Change `focus:ring-1` to `focus-visible:ring-1`
- Change `focus:ring-[var(--primary)]` to `focus-visible:ring-[var(--primary)]`

The resulting className string becomes:
`"flex h-9 w-full rounded-md border border-[var(--border)] bg-transparent px-3 py-1 text-sm shadow-sm transition-colors placeholder:text-[var(--muted-foreground)] focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-[var(--primary)] disabled:cursor-not-allowed disabled:opacity-50"`
  </action>
  <verify>
    <automated>grep -q "section-container" src/app/globals.css && grep -q "container-max-width" src/app/globals.css && grep -q "focus-visible:ring-1" src/components/ui/input.tsx && ! grep -q "focus:ring-1" src/components/ui/input.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `--container-width: 100dvw`
    - src/app/globals.css contains `--container-max-width: 1800px`
    - src/app/globals.css contains `--container-padding: 1.5rem`
    - src/app/globals.css contains `.section-container`
    - src/app/globals.css contains `margin-inline: auto`
    - src/components/ui/input.tsx contains `focus-visible:outline-none`
    - src/components/ui/input.tsx contains `focus-visible:ring-1`
    - src/components/ui/input.tsx does NOT contain bare `focus:ring-1` (without -visible)
    - src/components/ui/input.tsx does NOT contain bare `focus:outline-none` (without -visible)
  </acceptance_criteria>
  <done>.section-container utility is available in globals.css. Input component uses focus-visible for all focus ring styles.</done>
</task>

<task type="auto">
  <name>Task 4: Add CRON_SECRET auth guard to fetch endpoint</name>
  <files>src/app/api/fetch/route.ts</files>
  <read_first>
    - src/app/api/fetch/route.ts (current state — no auth check)
    - .planning/phases/01-foundation/01-RESEARCH.md (cron auth pattern with Bearer token)
    - vercel.json (cron config)
  </read_first>
  <action>
Modify `src/app/api/fetch/route.ts` to add CRON_SECRET authorization check as the first operation in the POST handler.

Change the function signature from `POST()` to `POST(request: Request)` so it receives the request object.

Add auth check at the top of the function body, before the `prisma.source.findMany()` call:

```typescript
const authHeader = request.headers.get("authorization");
if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
  return new Response("Unauthorized", { status: 401 });
}
```

The full function signature becomes:
```typescript
export async function POST(request: Request): Promise<Response> {
```

Do NOT change any other logic in the function. The existing source fetching, article deduplication, and error handling remain exactly as-is.
  </action>
  <verify>
    <automated>grep -q "CRON_SECRET" src/app/api/fetch/route.ts && grep -q "status: 401" src/app/api/fetch/route.ts && grep -q 'request: Request' src/app/api/fetch/route.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/fetch/route.ts contains `process.env.CRON_SECRET`
    - src/app/api/fetch/route.ts contains `status: 401`
    - src/app/api/fetch/route.ts contains `request: Request` parameter in POST function
    - src/app/api/fetch/route.ts contains `"Unauthorized"` response text
    - src/app/api/fetch/route.ts contains `authorization` header check
    - src/app/api/fetch/route.ts still contains `prisma.source.findMany()` (existing logic preserved)
    - src/app/api/fetch/route.ts still contains `Promise.allSettled` (existing logic preserved)
  </acceptance_criteria>
  <done>Cron endpoint requires valid CRON_SECRET Bearer token. Requests without it receive 401 Unauthorized. FOUND-03 satisfied.</done>
</task>

</tasks>

<verification>
- `grep -q "Geist Sans" CLAUDE.md` -- CLAUDE.md rewritten for this project
- `grep -q "Serena MCP" CLAUDE.md` -- Serena documented (DEV-04)
- `grep -q "investigation-mode" CLAUDE.md` -- Skills documented (DEV-03)
- `grep -q "section-container" src/app/globals.css` -- CSS utility available
- `grep -q "focus-visible:ring" src/components/ui/input.tsx` -- Input bug fixed
- `grep -q "CRON_SECRET" src/app/api/fetch/route.ts` -- Cron secured
- `npm run lint` -- No lint errors introduced
</verification>

<success_criteria>
- CLAUDE.md documents this project's actual conventions (Geist fonts, zinc palette, shadcn/ui components), not the Lego project
- CLAUDE.md Tool Preferences includes Serena MCP config details and npm run serena start command (DEV-04)
- CLAUDE.md Tool Preferences includes available skills: investigation-mode, cron-jobs (DEV-03)
- .serena/project.yml verified as correctly configured for this project
- .section-container utility exists in globals.css with correct token values
- Input component uses focus-visible everywhere (no bare focus:)
- POST /api/fetch returns 401 without valid Authorization header
- .claude/memory files are corrected for this project
- No lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
