---
phase: 03.2-neon-postgres-migration-vercel-deployment
plan: 03
type: execute
wave: 3
depends_on: ["03.2-02"]
files_modified:
  - vercel.json
  - src/app/api/articles/route.ts
  - prisma/seed.ts
autonomous: false
requirements:
  - D-14
  - D-15
  - D-16
  - D-17
  - D-19

must_haves:
  truths:
    - "Cron jobs run daily at 05:00 and 06:00 UTC"
    - "Article search is case-insensitive"
    - "Seed script runs against Postgres"
    - "Application deploys to Vercel and all pages load"
    - "Cron endpoints respond correctly on deployed instance"
  artifacts:
    - path: "vercel.json"
      provides: "Daily cron schedule at 05:00 and 06:00 UTC"
      contains: "0 5 * * *"
    - path: "src/app/api/articles/route.ts"
      provides: "Case-insensitive search via mode: insensitive"
      contains: "mode: 'insensitive'"
  key_links:
    - from: "vercel.json"
      to: "src/app/api/fetch/route.ts"
      via: "cron path /api/fetch"
      pattern: "/api/fetch"
    - from: "vercel.json"
      to: "src/app/api/enrich/route.ts"
      via: "cron path /api/enrich"
      pattern: "/api/enrich"
    - from: "src/app/api/articles/route.ts"
      to: "Neon Postgres"
      via: "Prisma contains with mode insensitive"
      pattern: "mode.*insensitive"
---

<objective>
Update the cron schedule to daily, add case-insensitive search, seed sources in Postgres, deploy to Vercel, and verify all pages and cron endpoints work.

Purpose: This is the deployment and final configuration plan. After this, the application runs entirely on Neon Postgres via Vercel with correct daily cron schedules and improved search.
Output: Live Vercel deployment with Postgres backend, daily crons, case-insensitive search.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-CONTEXT.md
@.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-RESEARCH.md
@.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-01-SUMMARY.md
@.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-02-SUMMARY.md

<interfaces>
<!-- Current state of files to be modified -->

From vercel.json (CURRENT - cron times to be updated):
```json
{
  "crons": [
    { "path": "/api/fetch", "schedule": "0 4 * * *" },
    { "path": "/api/enrich", "schedule": "0 5 * * *" }
  ]
}
```

From src/app/api/articles/route.ts (CURRENT - search block to add mode: insensitive):
```typescript
if (search) {
  andConditions.push({
    OR: [
      { title: { contains: search } },
      { source: { name: { contains: search } } },
    ],
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cron schedule, add case-insensitive search, seed Postgres</name>
  <read_first>
    - vercel.json
    - src/app/api/articles/route.ts
    - prisma/seed.ts
    - .planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-CONTEXT.md
  </read_first>
  <files>vercel.json, src/app/api/articles/route.ts, prisma/seed.ts</files>
  <action>
    **1. vercel.json** -- Update cron schedule per D-14:
    Change fetch from `"0 4 * * *"` to `"0 5 * * *"` and enrich from `"0 5 * * *"` to `"0 6 * * *"`:
    ```json
    {
      "crons": [
        { "path": "/api/fetch", "schedule": "0 5 * * *" },
        { "path": "/api/enrich", "schedule": "0 6 * * *" }
      ]
    }
    ```

    **2. src/app/api/articles/route.ts** -- Add `mode: 'insensitive'` per D-15, keep AND array composition per D-16:
    Change the search block (around line 24-31) to:
    ```typescript
    if (search) {
      andConditions.push({
        OR: [
          { title: { contains: search, mode: 'insensitive' } },
          { source: { name: { contains: search, mode: 'insensitive' } } },
        ],
      });
    }
    ```
    No other changes to this file. The AND array composition pattern is correct and stays (per D-16).

    **3. prisma/seed.ts** -- Run seed to populate Neon with source definitions:
    ```bash
    npm run seed
    ```
    The seed script uses `upsert` by URL, so it is safe to run even though sources were already migrated. It ensures all sources exist in Postgres and the TLDR URL-swap preamble runs correctly.
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/news-reader && npm run build 2>&1 | tail -3 && npm run seed 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json contains `"schedule": "0 5 * * *"` for /api/fetch
    - vercel.json contains `"schedule": "0 6 * * *"` for /api/enrich
    - src/app/api/articles/route.ts contains `mode: 'insensitive'` (appears twice, once per contains filter)
    - src/app/api/articles/route.ts still contains `andConditions.push` (AND composition preserved per D-16)
    - `npm run seed` exits 0 and outputs source names
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Cron schedule updated to 05:00/06:00 UTC daily, search is case-insensitive, sources seeded in Postgres</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel and verify CRON_SECRET</name>
  <read_first>
    - vercel.json
    - package.json
  </read_first>
  <files>N/A</files>
  <action>
    **1. Verify CRON_SECRET is set in Vercel environment variables:**
    ```bash
    vercel env ls
    ```
    Check output for `CRON_SECRET`. If missing, add it:
    ```bash
    vercel env add CRON_SECRET production
    ```
    (Enter a secure random value when prompted)

    Also ensure CRON_SECRET is set for Development environment if not already.

    **2. Deploy to production:**
    ```bash
    vercel deploy --prod
    ```

    This will:
    - Run `postinstall` (prisma generate)
    - Run `build` (prisma generate && prisma migrate deploy && next build)
    - Deploy the application
    - Register cron jobs from vercel.json

    **3. Verify deployment by checking the build log output** for:
    - "Prisma Migrate" step completing successfully
    - "Next.js build" completing successfully
    - No errors related to missing environment variables

    Per D-19: The Vercel project is already linked as vajkris-projects/news-reader.
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/news-reader && vercel deploy --prod 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `vercel deploy --prod` exits 0 and outputs a production URL
    - Build log shows "prisma migrate deploy" completing without errors
    - Build log shows "next build" completing without errors
    - CRON_SECRET is visible in `vercel env ls` output for Production environment
  </acceptance_criteria>
  <done>Application deployed to Vercel with Postgres backend, cron jobs registered, CRON_SECRET configured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deployed application</name>
  <files>N/A</files>
  <action>
    Claude provides the production URL from the deploy output and asks user to verify:
    1. Home/Feed page: Navigate to the production URL. Verify articles load with enrichment data.
    2. Briefing page: Navigate to /briefing. Verify topic groups and importance tiers display.
    3. Search: On the feed page, search for a term using mixed case (e.g., "claude" or "OPENAI"). Verify results appear regardless of case.
    4. Sources page: Navigate to /sources. Verify source list loads with article counts.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" $(vercel ls --json 2>/dev/null | head -1 | grep -o 'https://[^ ]*' | head -1) || echo "manual-verify"</automated>
  </verify>
  <done>User confirms all pages (feed, briefing, search, sources) load correctly on the deployed Vercel instance with migrated data from Neon Postgres</done>
</task>

</tasks>

<verification>
1. `vercel deploy --prod` succeeds with no errors
2. Production URL loads the feed page with articles
3. /briefing page shows topic-grouped articles
4. Search is case-insensitive (searching "claude" matches "Claude")
5. Cron jobs appear in Vercel dashboard (Settings > Cron Jobs)
6. `npm test` exits 0
7. `npm run build` exits 0
</verification>

<success_criteria>
- Vercel deployment live and serving all pages from Neon Postgres
- Cron jobs scheduled at 05:00 UTC (fetch) and 06:00 UTC (enrich)
- Case-insensitive search working on deployed instance
- CRON_SECRET configured in Vercel environment
- All migrated data visible in the deployed application
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-03-SUMMARY.md`
</output>
