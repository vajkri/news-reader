---
phase: 04.5-dev-debug-mode-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/debug.tsx
  - src/components/features/debug/DebugToggle.tsx
  - src/components/features/debug/index.ts
  - src/app/layout.tsx
  - src/types/index.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "A floating debug toggle button is visible in the bottom-right corner when running in dev mode"
    - "Clicking the toggle flips debug state between on and off"
    - "The toggle and debug provider produce zero footprint in production builds"
    - "Any client component in the tree can call useDebug() to read debug state"
  artifacts:
    - path: "src/contexts/debug.tsx"
      provides: "DebugProvider context + useDebug hook"
      exports: ["DebugProvider", "useDebug"]
    - path: "src/components/features/debug/DebugToggle.tsx"
      provides: "Floating fixed toggle button"
      exports: ["DebugToggle"]
    - path: "src/components/features/debug/index.ts"
      provides: "Barrel export for debug feature"
      exports: ["DebugToggle"]
    - path: "src/types/index.ts"
      provides: "ArticleRow with contentType and thinContent fields"
      contains: "contentType"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/contexts/debug.tsx"
      via: "DebugProvider wraps body content"
      pattern: "<DebugProvider>"
    - from: "src/app/layout.tsx"
      to: "src/components/features/debug/DebugToggle.tsx"
      via: "DebugToggle mounted inside DebugProvider"
      pattern: "<DebugToggle"
    - from: "src/components/features/debug/DebugToggle.tsx"
      to: "src/contexts/debug.tsx"
      via: "useDebug() hook call"
      pattern: "useDebug"
---

<objective>
Create the debug mode infrastructure: React Context provider with useDebug hook, floating toggle button, and layout wiring. Also extend ArticleRow type with contentType and thinContent fields for complete debug JSON output in Plan 02.

Purpose: Establishes the app-wide debug state mechanism that all future debug annotations will consume. Per D-04, uses React Context over HTML data attribute because debug mode has no CSS-only consumers and Server Components cannot read document attributes.

Output: DebugProvider, useDebug hook, DebugToggle button, updated ArticleRow type. Debug toggle visible at bottom-right in dev mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5-dev-debug-mode-infrastructure/04.5-CONTEXT.md
@.planning/phases/04.5-dev-debug-mode-infrastructure/04.5-RESEARCH.md
@.planning/phases/04.5-dev-debug-mode-infrastructure/04.5-UI-SPEC.md

<interfaces>
<!-- Existing code the executor needs to understand -->

From src/app/layout.tsx:
```typescript
// Server Component - no 'use client'
import { ChatWrapper } from "@/components/features/chat";
// Body structure:
// <body> -> <header> -> <div class="app-content"> -> <main>{children}</main> + <ChatWrapper />
```

From src/types/index.ts:
```typescript
export interface ArticleRow {
  id: number;
  guid: string;
  title: string;
  link: string;
  description: string | null;
  thumbnail: string | null;
  publishedAt: string | null;
  readTimeMin: number | null;
  isRead: boolean;
  summary: string | null;
  topics: Prisma.JsonValue | null;
  importanceScore: number | null;
  enrichedAt: string | null;
  sourceId: number;
  source: { name: string; category: string | null; };
}
// NOTE: contentType and thinContent exist in Prisma schema but are missing from this type
```

From src/components/ui/button.tsx:
```typescript
// Button component with variants: default, outline, ghost, destructive
// Sizes: default (h-9), sm (h-7), lg (h-11), icon (h-8 w-8)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DebugProvider context, useDebug hook, and DebugToggle button</name>
  <files>
    src/contexts/debug.tsx,
    src/components/features/debug/DebugToggle.tsx,
    src/components/features/debug/index.ts
  </files>
  <read_first>
    src/app/layout.tsx,
    src/components/features/chat/ChatWrapper.tsx,
    src/lib/scrape.ts,
    src/components/ui/button.tsx
  </read_first>
  <action>
    Create `src/contexts/debug.tsx` (per D-04: React Context chosen over data attribute):
    - Add `'use client'` directive
    - Define `DebugContextValue` interface with `debugMode: boolean` and `toggleDebug: () => void`
    - Create `DebugContext` with `createContext<DebugContextValue>({ debugMode: false, toggleDebug: () => {} })`
    - Export `DebugProvider` component that:
      - If `process.env.NODE_ENV !== 'development'`, returns `<>{children}</>` (per D-02: zero production footprint)
      - Otherwise renders `DebugProviderInner` which holds `useState(false)` for debugMode
    - Export `useDebug()` hook that returns `useContext(DebugContext)`
    - All exports must have explicit return types (project rule: strict TypeScript)

    Create `src/components/features/debug/DebugToggle.tsx` (per D-01, D-03):
    - Add `'use client'` directive
    - Import `useDebug` from `@/contexts/debug`
    - Import `Button` from `@/components/ui/button` (project rule: never raw button)
    - Import `Bug`, `BugOff` from `lucide-react`
    - Guard: if `process.env.NODE_ENV !== 'development'` return `null`
    - Call `useDebug()` to get `debugMode` and `toggleDebug`
    - Render `Button` with:
      - `variant="outline"` `size="icon"`
      - `onClick={toggleDebug}`
      - `className="fixed bottom-4 right-4 z-50 opacity-60 hover:opacity-100"` (per UI-SPEC)
      - When debug ON: add `opacity-100` class instead of `opacity-60`
      - `aria-label={debugMode ? 'Disable debug mode' : 'Enable debug mode'}` (per UI-SPEC copywriting)
      - `aria-pressed={debugMode}`
      - Show `BugOff` icon when debug ON, `Bug` icon when debug OFF, both `size={16}`
    - Explicit return type `React.ReactElement | null`

    Create `src/components/features/debug/index.ts`:
    - Single line: `export { DebugToggle } from './DebugToggle';`
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/conductor/workspaces/news-reader/amsterdam && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/contexts/debug.tsx` contains `export function DebugProvider`
    - `src/contexts/debug.tsx` contains `export function useDebug`
    - `src/contexts/debug.tsx` contains `process.env.NODE_ENV !== 'development'`
    - `src/contexts/debug.tsx` contains `'use client'`
    - `src/components/features/debug/DebugToggle.tsx` contains `import { useDebug } from '@/contexts/debug'`
    - `src/components/features/debug/DebugToggle.tsx` contains `import { Button } from '@/components/ui/button'`
    - `src/components/features/debug/DebugToggle.tsx` contains `Bug, BugOff`
    - `src/components/features/debug/DebugToggle.tsx` contains `fixed bottom-4 right-4 z-50`
    - `src/components/features/debug/DebugToggle.tsx` contains `aria-pressed={debugMode}`
    - `src/components/features/debug/DebugToggle.tsx` contains `'use client'`
    - `src/components/features/debug/index.ts` contains `export { DebugToggle }`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>DebugProvider, useDebug hook, and DebugToggle button exist. All lint and build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DebugProvider and DebugToggle into layout, extend ArticleRow type</name>
  <files>
    src/app/layout.tsx,
    src/types/index.ts
  </files>
  <read_first>
    src/app/layout.tsx,
    src/types/index.ts,
    src/contexts/debug.tsx,
    src/components/features/debug/DebugToggle.tsx,
    prisma/schema.prisma
  </read_first>
  <action>
    Modify `src/app/layout.tsx`:
    - Add imports: `import { DebugProvider } from '@/contexts/debug'` and `import { DebugToggle } from '@/components/features/debug'`
    - Wrap the content inside `<body>` with `<DebugProvider>...</DebugProvider>`:
      - `<DebugProvider>` goes inside `<body>`, wrapping `<header>`, `<div className="app-content">`, and the new `<DebugToggle />`
      - `<DebugToggle />` placed as last child inside `<DebugProvider>`, after the `<div className="app-content">` div
    - The resulting body structure:
      ```
      <body ...>
        <DebugProvider>
          <header>...</header>
          <div className="app-content">
            <main>{children}</main>
            <ChatWrapper />
          </div>
          <DebugToggle />
        </DebugProvider>
      </body>
      ```

    Modify `src/types/index.ts`:
    - Add `contentType: string | null;` field to `ArticleRow` interface (after `enrichedAt`)
    - Add `thinContent: boolean | null;` field to `ArticleRow` interface (after `contentType`)
    - These fields exist in the Prisma schema (`model Article`) but were never added to the shared type
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/conductor/workspaces/news-reader/amsterdam && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `import { DebugProvider } from '@/contexts/debug'`
    - `src/app/layout.tsx` contains `import { DebugToggle } from '@/components/features/debug'`
    - `src/app/layout.tsx` contains `<DebugProvider>`
    - `src/app/layout.tsx` contains `<DebugToggle`
    - `src/types/index.ts` contains `contentType: string | null`
    - `src/types/index.ts` contains `thinContent: boolean | null`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>DebugProvider wraps the app, DebugToggle renders in layout, ArticleRow includes contentType and thinContent. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes (no type errors, no import errors)
- `pnpm lint` passes (no ESLint violations)
- `src/contexts/debug.tsx` exists with DebugProvider and useDebug exports
- `src/components/features/debug/DebugToggle.tsx` exists with floating button
- `src/app/layout.tsx` wraps content with DebugProvider and mounts DebugToggle
- `src/types/index.ts` ArticleRow includes contentType and thinContent
</verification>

<success_criteria>
- Debug toggle button visible at bottom-right in `pnpm dev` (NODE_ENV=development)
- Clicking toggle changes icon between Bug and BugOff
- useDebug() hook returns { debugMode, toggleDebug } from any client component in the tree
- ArticleRow type includes contentType and thinContent for downstream debug JSON
- Zero production footprint: DebugProvider returns plain children, DebugToggle returns null
- pnpm build and pnpm lint both pass
</success_criteria>

<output>
After completion, create `.planning/phases/04.5-dev-debug-mode-infrastructure/04.5-01-SUMMARY.md`
</output>
