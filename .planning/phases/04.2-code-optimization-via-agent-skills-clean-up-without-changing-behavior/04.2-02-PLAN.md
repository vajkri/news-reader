---
phase: 04.2-code-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: true
requirements:
  - OPT-04

must_haves:
  truths:
    - "next.config.ts includes optimizePackageImports for lucide-react under experimental key"
    - "All existing lucide-react imports continue to work unchanged"
    - "Build succeeds with the new config"
  artifacts:
    - path: "next.config.ts"
      provides: "Bundle optimization for barrel imports"
      contains: "optimizePackageImports"
  key_links:
    - from: "next.config.ts"
      to: "lucide-react"
      via: "experimental.optimizePackageImports array"
      pattern: "optimizePackageImports.*lucide-react"
---

<objective>
Add optimizePackageImports to next.config.ts for lucide-react bundle optimization.

Purpose: Tree-shake lucide-react barrel imports at build time (per skill audit finding from react-best-practices/bundle-barrel-imports rule). No import syntax changes needed anywhere.
Output: Updated next.config.ts with experimental.optimizePackageImports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-code-optimization-via-agent-skills-clean-up-without-changing-behavior/04.2-CONTEXT.md
@.planning/phases/04.2-code-optimization-via-agent-skills-clean-up-without-changing-behavior/04.2-RESEARCH.md

<interfaces>
<!-- Current next.config.ts structure -->
From next.config.ts:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "**" },
      { protocol: "http", hostname: "**" },
    ],
  },
};

export default nextConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimizePackageImports to next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current config structure, need to add experimental key alongside images)
  </read_first>
  <action>
**Per D-06 skill audit: Add optimizePackageImports for lucide-react.**

Add the `experimental` key to `nextConfig` in `next.config.ts`. The final file should be:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    optimizePackageImports: ["lucide-react"],
  },
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**",
      },
      {
        protocol: "http",
        hostname: "**",
      },
    ],
  },
};

export default nextConfig;
```

**Do NOT:**
- Change any existing `images` configuration
- Add other packages to optimizePackageImports (only lucide-react has barrel import issues per audit)
- Modify any component import statements (optimizePackageImports works transparently)
- Touch the `unoptimized` flag on Image components (excluded by D-05)
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/news-reader && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains `experimental:` key
    - next.config.ts contains `optimizePackageImports: ["lucide-react"]`
    - next.config.ts still contains the existing `images` block with `remotePatterns`
    - `pnpm lint` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>next.config.ts has optimizePackageImports for lucide-react. Build succeeds. No import changes needed anywhere.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes (config is valid, all lucide-react imports resolve correctly)
- `pnpm lint` passes
- `next.config.ts` contains both `experimental.optimizePackageImports` and existing `images` config
</verification>

<success_criteria>
1. lucide-react barrel imports are optimized at build time via next.config.ts (per D-06 skill audit)
2. Build succeeds with no regressions
3. No component files modified
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-code-optimization-via-agent-skills-clean-up-without-changing-behavior/04.2-02-SUMMARY.md`
</output>
