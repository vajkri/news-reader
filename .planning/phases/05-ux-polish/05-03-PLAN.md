---
phase: 05-ux-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/features/sources/SourceList.tsx
  - src/components/features/sources/SourceForm.tsx
  - src/app/sources/page.tsx
  - src/components/features/chat/ChatPanel.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Sources page has SourceAvatar visual anchor for each source row"
    - "Sources delete uses inline confirmation (not window.confirm)"
    - "Chat panel goes full viewport width below 640px"
    - "Sources page is usable on mobile with no horizontal scrolling"
    - "Sources form fields stack vertically on mobile"
  artifacts:
    - path: "src/components/features/sources/SourceList.tsx"
      provides: "Refreshed source list with SourceAvatar and inline delete confirmation"
      exports: ["SourceList"]
    - path: "src/components/features/chat/ChatPanel.tsx"
      provides: "Mobile full-width at 640px threshold"
      exports: ["ChatPanel"]
  key_links:
    - from: "src/components/features/sources/SourceList.tsx"
      to: "src/components/ui/SourceAvatar.tsx"
      via: "import SourceAvatar"
      pattern: "SourceAvatar"
    - from: "src/components/features/chat/ChatPanel.tsx"
      to: "globals.css"
      via: "CSS grid layout"
      pattern: "isNarrowViewport"
---

<objective>
Polish the sources page with visual refresh and accessible delete confirmation, fix the chat panel mobile threshold to match the 640px breakpoint, and make the sources form responsive.

Purpose: Completes UX-01 (consistent design) for the sources view and UX-02 (mobile support) for chat and sources pages. These changes are independent of the feed overhaul and can run in parallel.

Output: 3 modified component files, 1 modified page, 1 CSS file update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-UI-SPEC.md
@.planning/phases/05-ux-polish/05-RESEARCH.md
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.claude/rules/design-system.md
@.claude/rules/tailwind-css-vars.md

<interfaces>
<!-- Interfaces from Plan 01 -->

From src/components/ui/SourceAvatar.tsx (created in Plan 01):
```typescript
export function SourceAvatar({ sourceName, size = 'md' }: { sourceName: string; size?: 'sm' | 'md' }): React.ReactElement;
```

From src/types/index.ts:
```typescript
export interface SourceRow {
  id: number;
  name: string;
  url: string;
  category: string | null;
  createdAt: string;
  _count: { articles: number };
}
```

From src/components/features/chat/ChatPanel.tsx (current narrow viewport check, line 137):
```typescript
setIsNarrowViewport(window.innerWidth <= 430);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sources page visual refresh with SourceAvatar and inline delete</name>
  <files>
    src/components/features/sources/SourceList.tsx,
    src/components/features/sources/SourceForm.tsx,
    src/app/sources/page.tsx
  </files>
  <read_first>
    src/components/features/sources/SourceList.tsx,
    src/components/features/sources/SourceForm.tsx,
    src/app/sources/page.tsx,
    src/components/ui/SourceAvatar.tsx,
    src/components/ui/button.tsx,
    .planning/phases/05-ux-polish/05-UI-SPEC.md,
    .claude/rules/design-system.md
  </read_first>
  <action>
    **A. Update SourceList.tsx** (per D-14, sources visual refresh):

    1. Add imports: `SourceAvatar` from `@/components/ui/SourceAvatar`, `useState` from `react`

    2. Replace `window.confirm()` with inline delete confirmation:
       - Add state: `const [deletingId, setDeletingId] = useState<number | null>(null);`
       - When trash button clicked: `setDeletingId(source.id)` instead of calling confirm
       - When `deletingId === source.id`, replace the trash button cell with confirmation UI:
         ```tsx
         <div className="flex items-center gap-2">
           <span className="text-sm text-(--foreground)">Delete source?</span>
           <Button variant="destructive" size="sm" onClick={async () => { await handleDelete(id); setDeletingId(null); }}>
             Delete source
           </Button>
           <Button variant="ghost" size="sm" onClick={() => setDeletingId(null)}>
             Keep source
           </Button>
         </div>
         ```
       - Update `handleDelete` to remove the `confirm()` call. Just do the fetch + onDeleted directly.
       - Use UI-SPEC copy: "Delete source" for confirm button, "Keep source" for cancel button

    3. Add SourceAvatar column:
       - Add a new first column to the table header: empty `<th>` with `w-14` (56px for avatar + padding)
       - Add corresponding cell in each row: `<td className="px-4 py-3"><SourceAvatar sourceName={source.name} size="sm" /></td>`

    4. Update empty state copy per UI-SPEC: "No sources added yet. Add your first RSS feed to start fetching articles."

    5. Improve spacing: increase row padding from `py-3` to `py-3` (already correct), ensure `font-medium` on source name

    6. Mobile responsive: Add `overflow-x-auto` wrapper on the table for small screens. Alternatively, hide the URL column on mobile: URL `<th>` and `<td>` get `hidden sm:table-cell` class. This prevents horizontal scroll.

    **B. Update SourceForm.tsx** (per D-14):
    - The form already uses `flex flex-wrap gap-2` which handles wrapping
    - Make inputs stack on mobile: add `w-full sm:flex-1 sm:basis-36` to name input (replace `min-w-0 flex-1 basis-36`)
    - Same pattern for URL: `w-full sm:flex-[2] sm:basis-48`
    - Same for category: `w-full sm:flex-1 sm:basis-32`
    - Button: `w-full sm:w-auto`
    - This ensures all fields stack vertically on mobile, side-by-side on desktop

    **C. Update sources/page.tsx**:
    - Update page description per UI-SPEC copy: Keep existing heading "RSS Sources" and description.
    - If description doesn't match UI-SPEC copy, update to: "Manage the RSS feeds your reader pulls from."
    - (Current description is already correct, just verify)
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SourceList.tsx imports `SourceAvatar` from `@/components/ui/SourceAvatar`
    - SourceList.tsx imports `useState` from `react`
    - SourceList.tsx contains `deletingId` state variable
    - SourceList.tsx does NOT contain `window.confirm` or bare `confirm(`
    - SourceList.tsx contains text "Delete source" and "Keep source" for confirmation buttons
    - SourceList.tsx renders `<SourceAvatar` in each row
    - SourceList.tsx contains `hidden sm:table-cell` on URL column for mobile
    - SourceForm.tsx contains `w-full sm:flex-1` or `w-full sm:flex-[2]` responsive classes
    - SourceList empty state contains "No sources added yet"
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Sources page has SourceAvatar visual anchor, accessible inline delete confirmation, and mobile-responsive form and table.</done>
</task>

<task type="auto">
  <name>Task 2: Fix chat panel mobile threshold to 640px</name>
  <files>
    src/components/features/chat/ChatPanel.tsx
  </files>
  <read_first>
    src/components/features/chat/ChatPanel.tsx,
    .planning/phases/05-ux-polish/05-UI-SPEC.md,
    .planning/phases/05-ux-polish/05-RESEARCH.md
  </read_first>
  <action>
    Update the narrow viewport detection in ChatPanel.tsx (per D-06):

    1. Find the `checkWidth` function (around line 136-137):
       ```typescript
       const checkWidth = (): void => {
         setIsNarrowViewport(window.innerWidth <= 430);
       };
       ```

    2. Change the threshold from `430` to `639`:
       ```typescript
       const checkWidth = (): void => {
         setIsNarrowViewport(window.innerWidth < 640);
       };
       ```
       Use `< 640` (strict less-than) to match Tailwind's `sm` breakpoint at 640px. At exactly 640px, the viewport is NOT narrow (desktop behavior applies).

    This is the only change needed for chat mobile. When `isNarrowViewport` is true:
    - Panel width becomes `100%` (line ~300)
    - Resize handle is hidden (line ~324)
    - These behaviors are already implemented, just triggered at the wrong threshold
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ChatPanel.tsx contains `window.innerWidth < 640` (not `<= 430`)
    - ChatPanel.tsx does NOT contain `430` as a viewport threshold
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Chat panel goes full viewport width below 640px, matching the Tailwind `sm` breakpoint used by the rest of the responsive layout.</done>
</task>

<task type="auto">
  <name>Task 3: Typography audit and dark mode pass</name>
  <files>
    src/app/globals.css,
    src/components/features/sources/SourceList.tsx,
    src/components/features/sources/SourceForm.tsx,
    src/components/features/chat/ChatPanel.tsx
  </files>
  <read_first>
    src/app/globals.css,
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/sources/page.tsx,
    src/app/briefing/page.tsx,
    src/components/features/feed/FeedTable.tsx,
    src/components/features/feed/columns.tsx,
    src/components/features/briefing/BriefingCard.tsx,
    src/components/features/briefing/TopicGroup.tsx,
    src/components/features/chat/ChatMessage.tsx,
    .claude/rules/design-system.md,
    .planning/phases/05-ux-polish/05-UI-SPEC.md
  </read_first>
  <action>
    **A. Typography audit** (per D-08, D-10):

    Read every component listed in read_first and verify against the design system typography scale:
    - Body (16px, 400): `text-base font-normal` -- article descriptions, summaries, primary reading text
    - UI Label (14px, 600): `text-sm font-semibold` -- button labels, nav links, badge text, section headings in toolbar
    - Small/Metadata (14px, 400): `text-sm font-normal` -- article metadata, timestamps, filter labels
    - Heading (18px, 600): `text-lg font-semibold` -- page titles, section headers
    - Mono (13px, 400): `font-mono text-[13px]` -- code references, URL display

    Fix any drift:
    - If any heading uses `text-xl` or larger, change to `text-lg font-semibold`
    - If any body text uses a size smaller than `text-sm` (except badges at `text-xs`), increase to `text-sm`
    - If any metadata uses `text-[13px]` or `text-[11px]`, change to `text-xs` (12px, badge exception) or `text-sm` (14px)
    - Ensure all source names, timestamps, category badges use appropriate tokens

    Text color hierarchy audit (per D-10):
    - Primary text (titles, headings): must use `text-(--foreground)`, not `text-gray-900` or hardcoded
    - Secondary text (summaries, insights): must use `text-(--foreground-secondary)`, not `text-gray-700`
    - Metadata (timestamps, source names): must use `text-(--muted-foreground)`, not `text-gray-500`
    - If any component uses hardcoded Tailwind color classes for text (e.g., `text-zinc-700`), replace with CSS variable tokens

    **B. Dark mode audit** (per D-11):

    Verify the following in globals.css (read and confirm values are correct):
    - `--card: #0f0f12` is distinct from `--background: #09090b` (confirmed, already different)
    - `--border: #27272a` is present in dark mode
    - `--foreground-secondary: #d4d4d8` is defined
    - `--muted-foreground: #a1a1aa` is defined

    Scan all modified and existing components for dark mode issues:
    - Any hardcoded light-mode-only colors (e.g., `text-blue-600` without `dark:text-blue-400` pair)
    - In FeedTable status bar: `text-blue-600 dark:text-blue-400` on unread count -- this is acceptable (functional color, not token)
    - In FeedTable status bar: `text-green-600 dark:text-green-400` on fetch result -- acceptable
    - In columns.tsx: `text-blue-600 dark:text-blue-400` on unread Circle icon -- acceptable
    - In FeedTable error: `text-red-600 dark:text-red-400` -- acceptable
    - Verify Source Avatar backgrounds (600-level): these don't change in dark mode and `text-white` on 600-level bg passes 4.5:1. No change needed.
    - Verify NEW badge: Badge `secondary` variant uses `--secondary` bg and `--foreground` text. In dark mode, `--secondary: #27272a` bg with `--foreground: #fafafa` text. This is accessible.

    Fix any issues found. If all tokens are correctly applied, note that the audit passed with no changes needed in the summary.

    Common issues to fix if found:
    - `text-gray-*` classes -> replace with `text-(--muted-foreground)` or `text-(--foreground-secondary)`
    - `bg-gray-*` classes -> replace with `bg-(--muted)` or `bg-(--secondary)`
    - Missing `dark:` variants on any hardcoded colors that aren't functional status colors
  </action>
  <verify>
    <automated>pnpm build && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - No `text-gray-` classes in any modified component (except inside node_modules or third-party)
    - No `bg-gray-` classes in any modified component
    - All page headings use `text-lg font-semibold`
    - All metadata text uses `text-(--muted-foreground)` or `text-xs text-(--muted-foreground)`
    - globals.css dark mode block contains `--card: #0f0f12` and `--foreground-secondary: #d4d4d8`
    - `pnpm build` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Typography scale enforced across all views. Dark mode audit completed with all contrast ratios verified. No hardcoded color values remain.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm lint` passes
- Sources page shows SourceAvatar in each row
- Sources delete uses inline confirmation (no window.confirm)
- Chat panel goes full-width below 640px
- Typography uses correct scale across all views
- No dark mode contrast issues
</verification>

<success_criteria>
- Sources page visually refreshed with avatars and accessible delete confirmation
- Sources form stacks on mobile
- Chat panel full-width below 640px
- Typography scale enforced (D-08)
- Dark mode audit completed (D-11)
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-03-SUMMARY.md`
</output>
