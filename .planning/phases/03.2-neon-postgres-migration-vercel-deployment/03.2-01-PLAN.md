---
phase: 03.2-neon-postgres-migration-vercel-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/prisma.ts
  - src/lib/enrich.ts
  - src/lib/briefing.ts
  - src/types/index.ts
  - src/lib/enrich.test.ts
  - src/lib/briefing.test.ts
  - package.json
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-20
user_setup:
  - service: neon
    why: "Postgres database for production and development"
    env_vars:
      - name: DATABASE_URL
        source: "Auto-injected by Vercel-Neon integration (pooled connection)"
      - name: DATABASE_URL_UNPOOLED
        source: "Auto-injected by Vercel-Neon integration (direct connection)"
    dashboard_config:
      - task: "Provision Neon via Vercel Marketplace"
        location: "Vercel Dashboard > Project > Storage > Create Database > Neon Postgres"

must_haves:
  truths:
    - "Prisma schema uses postgresql provider with pooled URL and directUrl"
    - "Fresh Postgres migration exists (old SQLite migrations deleted)"
    - "topics field is Json? throughout the codebase (no JSON.stringify/parse workarounds)"
    - "All existing unit tests pass with updated fixtures"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Postgres schema with Json? topics field"
      contains: 'provider  = "postgresql"'
    - path: "prisma/migrations/"
      provides: "Single fresh init migration for Postgres"
    - path: "src/lib/enrich.ts"
      provides: "Enrichment save without JSON.stringify on topics"
    - path: "src/lib/briefing.ts"
      provides: "parseTopics accepting JsonValue instead of string"
    - path: "src/types/index.ts"
      provides: "ArticleRow with JsonValue topics type"
  key_links:
    - from: "src/lib/enrich.ts"
      to: "prisma/schema.prisma"
      via: "topics field written as array directly"
      pattern: "topics: result\\.topics"
    - from: "src/lib/briefing.ts"
      to: "src/types/index.ts"
      via: "ArticleRow.topics type"
      pattern: "parseTopics.*JsonValue"
---

<objective>
Provision Neon Postgres via Vercel Marketplace, migrate the Prisma schema from SQLite to PostgreSQL, propagate the topics Json? type change across all source files and tests, and run the initial Postgres migration.

Purpose: This is the foundational database switch that every subsequent plan depends on. Without Postgres running and the schema migrated, no data migration or deployment can happen.
Output: Working Postgres schema, updated Prisma client, all code and tests compatible with Json? topics type, passing test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-CONTEXT.md
@.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From prisma/schema.prisma (CURRENT - to be replaced):
```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
// topics is String? on Article model
```

From src/types/index.ts (CURRENT - to be updated):
```typescript
export interface ArticleRow {
  // ...
  topics: string | null;  // CHANGE TO: Prisma.JsonValue | null
  // ...
}
```

From src/lib/enrich.ts L107 (CURRENT - to be updated):
```typescript
topics: JSON.stringify(result.topics),  // CHANGE TO: topics: result.topics
```

From src/lib/briefing.ts L16 (CURRENT - to be updated):
```typescript
export function parseTopics(raw: string | null): string[]  // CHANGE signature + remove JSON.parse
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision Neon Postgres via Vercel Marketplace</name>
  <files>.env.local</files>
  <action>
    Per D-01, D-02, D-03, D-20: The user provisions Neon Postgres through the Vercel Marketplace.

    Steps:
    1. Go to https://vercel.com/dashboard
    2. Select the vajkris-projects/news-reader project (per D-19)
    3. Click the Storage tab
    4. Click "Create Database" (or "Connect Store")
    5. Select Neon Postgres from the marketplace
    6. Name the database (e.g., "news-reader-db"), select EU region (per D-03), confirm Free tier (per D-02)
    7. Click "Connect to Project"; select Production + Development environments (NOT Preview, per D-04)
    8. Vercel injects DATABASE_URL, DATABASE_URL_UNPOOLED, and legacy POSTGRES_* vars into the project

    After provisioning, run locally:
    ```
    vercel env pull .env.local
    ```

    Verify .env.local now contains lines like:
    ```
    DATABASE_URL="postgresql://..."
    DATABASE_URL_UNPOOLED="postgresql://..."
    ```

    Also ensure CRON_SECRET is still present in .env.local (add it back if vercel env pull overwrote it).
  </action>
  <verify>
    <automated>grep -c "DATABASE_URL" .env.local</automated>
  </verify>
  <done>.env.local contains DATABASE_URL and DATABASE_URL_UNPOOLED pointing to Neon Postgres</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Prisma schema to PostgreSQL and run fresh migration</name>
  <read_first>
    - prisma/schema.prisma
    - src/lib/prisma.ts
    - package.json
    - .planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-RESEARCH.md
  </read_first>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/prisma.ts, package.json</files>
  <action>
    Per D-06, D-07:

    1. Update `prisma/schema.prisma` datasource block to:
    ```prisma
    datasource db {
      provider  = "postgresql"
      url       = env("DATABASE_URL")
      directUrl = env("DATABASE_URL_UNPOOLED")
    }
    ```

    2. Change the `topics` field on the Article model from `String?` to `Json?`.

    3. Delete the entire `prisma/migrations/` directory (contains SQLite-incompatible migrations: `20260318072441_init` and `20260319183507_add_enrichment_fields` and `migration_lock.toml`):
    ```bash
    rm -rf prisma/migrations
    ```

    4. Run fresh migration against the Neon Postgres database:
    ```bash
    npx prisma migrate dev --name init
    ```
    This creates a new `prisma/migrations/TIMESTAMP_init/migration.sql` with Postgres-compatible DDL.

    5. Add `"postinstall": "prisma generate"` to `package.json` scripts (safety net for Vercel build cache per Research Pattern 3). Place it after `"test"` in the scripts object.

    6. The `src/lib/prisma.ts` singleton pattern needs NO changes per D-05 (standard Prisma pooled connection, no adapter needed). The existing code works as-is with the Neon pooled URL.
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/news-reader && npx prisma validate && ls prisma/migrations/*/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `provider  = "postgresql"`
    - prisma/schema.prisma contains `directUrl = env("DATABASE_URL_UNPOOLED")`
    - prisma/schema.prisma contains `topics          Json?`
    - prisma/migrations/ contains exactly one subdirectory with `_init` suffix
    - prisma/migrations/*_init/migration.sql contains `CREATE TABLE "Article"` (Postgres DDL)
    - prisma/migrations/*_init/migration.sql does NOT contain `sqlite` anywhere
    - package.json scripts contains `"postinstall": "prisma generate"`
    - `npx prisma validate` exits 0
    - Old migration directories (20260318072441_init, 20260319183507_add_enrichment_fields) do NOT exist
  </acceptance_criteria>
  <done>Prisma schema is PostgreSQL with Json? topics, fresh init migration exists, postinstall script added</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Propagate topics Json? type change across source files and tests</name>
  <read_first>
    - src/lib/enrich.ts
    - src/lib/briefing.ts
    - src/types/index.ts
    - src/lib/enrich.test.ts
    - src/lib/briefing.test.ts
    - .planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-RESEARCH.md
  </read_first>
  <files>src/lib/enrich.ts, src/lib/briefing.ts, src/types/index.ts, src/lib/enrich.test.ts, src/lib/briefing.test.ts</files>
  <behavior>
    - enrich.test.ts: saveEnrichmentResults writes topics as plain array `['model releases', 'open source']` (not JSON.stringify'd string)
    - briefing.test.ts: parseTopics accepts arrays directly: `parseTopics(['model releases', 'developer tools'])` returns `['model releases', 'developer tools']`
    - briefing.test.ts: parseTopics(null) still returns ['Uncategorized']
    - briefing.test.ts: parseTopics([]) returns ['Uncategorized']
    - briefing.test.ts: groupArticlesByTopic fixtures use array values for topics (not JSON strings)
  </behavior>
  <action>
    Per D-07, Research Pattern 6 (topics field type change full impact map):

    **1. src/types/index.ts** -- Update ArticleRow.topics type:
    - Add import: `import type { Prisma } from '@prisma/client';`
    - Change `topics: string | null` to `topics: Prisma.JsonValue | null`

    **2. src/lib/enrich.ts** L107 -- Remove JSON.stringify wrapper:
    - Change `topics: JSON.stringify(result.topics),` to `topics: result.topics,`
    - No other changes needed in this file

    **3. src/lib/briefing.ts** -- Update parseTopics signature and body:
    - Add import: `import type { Prisma } from '@prisma/client';`
    - Change signature from `parseTopics(raw: string | null): string[]` to `parseTopics(raw: Prisma.JsonValue | null): string[]`
    - Replace body with:
    ```typescript
    if (!raw) return ['Uncategorized'];
    if (Array.isArray(raw) && raw.length > 0) return raw as string[];
    return ['Uncategorized'];
    ```
    - Remove the try/catch JSON.parse block entirely

    **4. src/lib/enrich.test.ts** -- Update test at L102:
    - Change test name from `'calls prisma.article.update with JSON.stringify on topics'` to `'calls prisma.article.update with topics array directly'`
    - Change expected topics value from `'["model releases","open source"]'` (string) to `['model releases', 'open source']` (array)

    **5. src/lib/briefing.test.ts** -- Update all test fixtures:
    - `parseTopics` tests: Replace string inputs with array inputs:
      - Remove test for empty string `parseTopics('')` (not applicable to JsonValue)
      - Remove test for non-JSON string `parseTopics('not json')` (not applicable to JsonValue)
      - Remove test for empty array JSON string `parseTopics('[]')` (not applicable)
      - Add test: `parseTopics([])` returns `['Uncategorized']`
      - Change `parseTopics('["model releases","developer tools"]')` to `parseTopics(['model releases', 'developer tools'])`
      - Change `parseTopics('["AI safety"]')` to `parseTopics(['AI safety'])`
    - `groupArticlesByTopic` tests: Change all `topics:` fixture values from JSON strings to arrays:
      - `'["AI safety","model releases"]'` becomes `['AI safety', 'model releases']`
      - `'["model releases"]'` becomes `['model releases']`
      - `'["AI safety"]'` becomes `['AI safety']`
      - `'["hardware"]'` becomes `['hardware']`
    - `makeArticle` helper: Update topics default type; keep `topics: null` default (still valid for JsonValue)
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/news-reader && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts contains `import type { Prisma } from '@prisma/client'`
    - src/types/index.ts contains `topics: Prisma.JsonValue | null`
    - src/lib/enrich.ts line with topics does NOT contain `JSON.stringify`
    - src/lib/enrich.ts contains `topics: result.topics,` (no wrapper)
    - src/lib/briefing.ts contains `parseTopics(raw: Prisma.JsonValue | null)`
    - src/lib/briefing.ts does NOT contain `JSON.parse`
    - src/lib/briefing.ts contains `Array.isArray(raw)`
    - src/lib/enrich.test.ts contains `topics: ['model releases', 'open source']` (array, not string)
    - src/lib/briefing.test.ts contains `parseTopics(['model releases', 'developer tools'])` (array input)
    - src/lib/briefing.test.ts does NOT contain `parseTopics('"` (no JSON string inputs)
    - `npm test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All source files and tests updated for Json? topics type. No JSON.stringify on write, no JSON.parse on read. All tests green.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` exits 0
2. `npm test` exits 0 (all unit tests pass with updated fixtures)
3. `npm run build` exits 0 (full Next.js build succeeds against Postgres)
4. prisma/schema.prisma has `provider = "postgresql"` and `topics Json?`
5. No `JSON.stringify` or `JSON.parse` calls on the `topics` field remain in src/
</verification>

<success_criteria>
- Neon Postgres provisioned via Vercel Marketplace with env vars pulled locally
- Prisma schema points to Postgres with directUrl for migrations
- Fresh init migration created (no SQLite migration artifacts remain)
- topics field is Json? in schema, Prisma.JsonValue in TypeScript, array in tests
- All 12+ existing unit tests pass with updated fixtures
- Build succeeds end to end
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-neon-postgres-migration-vercel-deployment/03.2-01-SUMMARY.md`
</output>
