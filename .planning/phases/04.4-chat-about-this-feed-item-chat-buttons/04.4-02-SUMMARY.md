# Plan 04.4-02 Summary: "Read this for me" URL-Fetch Tool

**Commit:** e107823
**Files changed:** 2 (+96/-1)

## Accomplishments

- Created `fetchArticleContentTool` in `chat-tools.ts` using cheerio for HTML content extraction:
  - Fetches article URL with 10-second timeout and bot user-agent header
  - Strips non-content elements (scripts, nav, ads, sidebars, social sharing)
  - Extracts readable text from `<article>`, `[role="main"]`, `<main>`, or common content class selectors
  - Truncates to 8000 characters to stay within token limits
  - Returns structured response with `title`, `url`, `content`, `error` fields
  - Graceful error handling for fetch failures, paywalls, and thin content (< 100 chars)
- Wired `fetchArticleContent` tool into the chat API route alongside existing search tools
- Updated `stepCountIs(3)` to `stepCountIs(4)` to support the search-then-fetch flow (search -> result -> fetch -> summarize)
- Added "Read this for me" section to SYSTEM_PROMPT with ADHD-friendly summary guidance:
  - Bold lead sentence, 3-5 short paragraphs, bullet points for lists, "Why it matters" closing
  - Target: 1-2 minute read
  - Fallback to existing enrichment summary if fetch fails

## Files Modified

- `src/lib/chat-tools.ts` -- New fetchArticleContentTool export
- `src/app/api/chat/route.ts` -- Tool registration, step count update, system prompt guidance
