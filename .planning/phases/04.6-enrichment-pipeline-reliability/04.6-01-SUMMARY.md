---
phase: 04.6-enrichment-pipeline-reliability
plan: "01"
subsystem: database-schema, enrichment-pipeline
tags: [prisma, migration, enrich, batch-size, ordering, staleness]
dependency_graph:
  requires: []
  provides: [UserPreference-model, ArticleFeedback-model, enrichment-fixes]
  affects: [src/lib/enrich.ts, prisma/schema.prisma, src/types/index.ts]
tech_stack:
  added: []
  patterns: [prisma-migrate-deploy, staleness-cutoff, newest-first-ordering]
key_files:
  created:
    - prisma/migrations/20260329000000_add_user_preference_and_article_feedback/migration.sql
  modified:
    - prisma/schema.prisma
    - src/types/index.ts
    - src/lib/enrich.ts
    - src/lib/enrich.test.ts
decisions:
  - "Migration applied via prisma migrate deploy (non-interactive env); migration SQL hand-authored from schema diff"
  - "createdAt used for staleness filter (not publishedAt): createdAt is row-insert time, publishedAt is article's original publication date"
  - "48h staleness cutoff: articles not enriched within 48h of fetch are abandoned as stale"
metrics:
  duration_min: 10
  completed_date: "2026-03-29"
  tasks_completed: 2
  files_modified: 4
---

# Phase 04.6 Plan 01: Schema Migration and Enrichment Pipeline Fixes Summary

**One-liner:** Added UserPreference/ArticleFeedback Prisma models, increased BATCH_LIMIT to 25, switched enrichment to newest-first with 48h staleness cutoff.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Schema migration for UserPreference and ArticleFeedback | 29281c4 | prisma/schema.prisma, src/types/index.ts, prisma/migrations/... |
| 2 | Enrichment pipeline fixes and test updates | d05b61e | src/lib/enrich.ts, src/lib/enrich.test.ts |

## What Was Built

### Task 1: Schema Migration
- Added `UserPreference` model (key/value store, will hold `briefing_watermark` per D-02)
- Added `ArticleFeedback` model with denormalized `sourceId`, `topics`, `contentType` fields for pattern extraction (per D-18)
- Added `feedback ArticleFeedback[]` back-relations to `Article` and `Source` models
- Added `createdAt: string` to `ArticleRow` TypeScript interface (was in Prisma schema but missing from TS type)
- Migration applied to Neon Postgres via `prisma migrate deploy`

### Task 2: Enrichment Pipeline Fixes
- `BATCH_LIMIT`: 15 -> 25 (per D-09)
- `fetchUnenrichedArticles`: added 48h `createdAt gte cutoff` staleness filter (per D-11)
- `fetchUnenrichedArticles`: changed `orderBy publishedAt asc` -> `publishedAt desc` (newest first, per D-12)
- Updated BATCH_LIMIT test assertion from `toBe(15)` to `toBe(25)`
- Added new `fetchUnenrichedArticles` describe block testing query shape (orderBy, where, take)

## Deviations from Plan

### Auto-fixed Issues

None.

### Process Deviations

**1. [Rule 3 - Blocking] Migration applied via prisma migrate deploy instead of migrate dev**

- **Found during:** Task 1
- **Issue:** `prisma migrate dev` requires interactive terminal; non-interactive CI/CD environment returned error "Prisma Migrate has detected that the environment is non-interactive"
- **Fix:** Hand-authored migration SQL file in `prisma/migrations/20260329000000_add_user_preference_and_article_feedback/migration.sql` (using `prisma migrate diff --from-empty` as reference for correct Postgres DDL syntax), then applied via `prisma migrate deploy` which works non-interactively
- **Files modified:** prisma/migrations/20260329000000_add_user_preference_and_article_feedback/migration.sql
- **Commit:** 29281c4

## Verification Results

- `pnpm prisma migrate deploy`: All migrations applied successfully
- `pnpm prisma generate`: Prisma Client generated
- `pnpm test src/lib/enrich.test.ts`: 13/13 tests pass
- `pnpm build`: Build succeeds, all routes compile

## Known Stubs

None. All schema models are empty tables with no data yet (populated by future plans: watermark in 04.6-02, feedback in 04.6-05).

## Self-Check: PASSED
