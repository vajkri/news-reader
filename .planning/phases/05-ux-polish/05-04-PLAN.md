---
phase: 05-ux-polish
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/components/features/briefing/BriefingCard.tsx
  - src/components/features/briefing/DateStepper.tsx
  - src/components/features/briefing/StatusBar.tsx
  - src/components/features/briefing/PendingSection.tsx
  - src/components/features/briefing/CaughtUpState.tsx
  - src/app/briefing/page.tsx
autonomous: false
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Briefing page renders correctly on mobile (375px) with no horizontal scrolling"
    - "BriefingCard has comfortable padding on mobile (not cramped by left border)"
    - "DateStepper buttons are touch-friendly on mobile"
    - "All 5 briefing states render correctly on mobile: morning visit, evening visit, caught up, archive, pending enrichment"
    - "Holistic UX audit confirms consistent design across all views"
  artifacts:
    - path: "src/components/features/briefing/BriefingCard.tsx"
      provides: "Mobile-responsive briefing card"
    - path: "src/components/features/briefing/DateStepper.tsx"
      provides: "Touch-friendly date navigation"
  key_links:
    - from: "src/app/briefing/page.tsx"
      to: "src/components/features/briefing/*"
      via: "Server Component rendering all briefing sub-components"
      pattern: "TopicGroup|BriefingCard|DateStepper|StatusBar"
---

<objective>
Polish the briefing page for mobile responsiveness, audit all 5 briefing states (D-16), and perform the holistic frontend-design audit across the entire application (D-15). Final visual verification checkpoint.

Purpose: This is the final polish pass. The briefing is the product's core view and needs to work perfectly on mobile. The holistic audit catches any remaining inconsistencies across views.

Output: Modified briefing components with mobile improvements, final checkpoint for visual approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-UI-SPEC.md
@.planning/phases/05-ux-polish/05-RESEARCH.md
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-02-SUMMARY.md
@.planning/phases/05-ux-polish/05-03-SUMMARY.md
@.claude/rules/design-system.md
@.claude/rules/tailwind-css-vars.md
@.planning/assets/briefing-mockup.html

<interfaces>
<!-- Briefing component interfaces from codebase -->

From src/lib/briefing.ts:
```typescript
export interface BriefingArticle { ... }
export interface TopicGroupData {
  topic: string;
  articles: BriefingArticle[];
}
```

From src/components/features/briefing/BriefingCard.tsx:
```typescript
interface BriefingCardProps {
  article: BriefingArticle;
  isNew?: boolean;
}
```

From src/components/features/briefing/StatusBar.tsx:
```typescript
interface StatusBarProps {
  newCount: number;
  lastVisit: Date;
  lastEnrichedAt: string | null;
  pendingCount: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Briefing mobile polish and all-states audit</name>
  <files>
    src/components/features/briefing/BriefingCard.tsx,
    src/components/features/briefing/DateStepper.tsx,
    src/components/features/briefing/StatusBar.tsx,
    src/components/features/briefing/PendingSection.tsx,
    src/components/features/briefing/CaughtUpState.tsx,
    src/app/briefing/page.tsx
  </files>
  <read_first>
    src/components/features/briefing/BriefingCard.tsx,
    src/components/features/briefing/DateStepper.tsx,
    src/components/features/briefing/StatusBar.tsx,
    src/components/features/briefing/PendingSection.tsx,
    src/components/features/briefing/CaughtUpState.tsx,
    src/components/features/briefing/ArchiveBanner.tsx,
    src/components/features/briefing/SectionDivider.tsx,
    src/components/features/briefing/TopicGroup.tsx,
    src/components/features/briefing/TriageSection.tsx,
    src/app/briefing/page.tsx,
    .planning/phases/05-ux-polish/05-UI-SPEC.md,
    .claude/rules/design-system.md
  </read_first>
  <action>
    **A. BriefingCard mobile polish** (per D-07, D-16):
    - Read BriefingCard.tsx. It uses `border-l-[3.5px] pl-6` for the left importance border.
    - At 375px, `pl-6` (24px) padding on top of container padding may feel cramped.
    - Adjust: change `pl-6` to `pl-4 sm:pl-6` (16px mobile, 24px desktop). This gives the card breathing room on mobile.
    - If the card has any fixed-width elements that could cause overflow at 375px, add `min-w-0` to flex children.
    - Ensure title uses `text-lg font-semibold` (18px heading).
    - Ensure summary/takeaway text uses `text-(--foreground-secondary)` per design tokens.
    - Verify the card's badges (New, importance tier) don't overflow on narrow screens. Add `flex-wrap` to the badge container if needed.

    **B. DateStepper touch targets**:
    - Read DateStepper.tsx. The navigation buttons (prev/next day) need minimum 44px touch targets on mobile.
    - If buttons use `size="icon"` (h-8 w-8 = 32px), change to `size="icon" className="h-8 w-8 sm:h-8 sm:w-8 min-h-[44px] min-w-[44px] sm:min-h-0 sm:min-w-0"` -- or simpler: just ensure the button's clickable area is 44px on mobile. Use `min-h-[44px] min-w-[44px]` on buttons with `sm:min-h-0 sm:min-w-0` to restore desktop size.
    - Alternatively, if the date display and buttons are in a flex row, ensure the overall row doesn't overflow at 375px. The date text may need to be abbreviated on mobile: e.g., `format(date, 'MMM d')` instead of a longer format.

    **C. StatusBar mobile**:
    - Read StatusBar.tsx. It likely uses a horizontal flex layout for status indicators.
    - Ensure it wraps on mobile: `flex flex-wrap gap-2` or `flex flex-col sm:flex-row gap-2`.
    - Text should be `text-sm` minimum, not smaller.

    **D. PendingSection mobile**:
    - Read PendingSection.tsx. Ensure the "Awaiting enrichment" labels and article titles don't overflow on narrow screens.
    - Add `min-w-0` and `truncate` or `line-clamp-2` on article titles if not already present.

    **E. CaughtUpState mobile**:
    - Read CaughtUpState.tsx. Ensure centered content with adequate padding on mobile.
    - The "You are caught up." heading and body text should be centered and readable at 375px.

    **F. Briefing page.tsx**:
    - The page header uses `flex items-center justify-between mb-8`.
    - On very narrow screens, if the DateStepper is too wide, the header could overflow.
    - Add `flex-wrap gap-4` to the header container as a safety measure: `flex items-center justify-between flex-wrap gap-4 mb-8`.
    - Verify `reading-container` (1024px max) with `padding-inline: 1.5rem` provides enough room at 375px. Effective content width = 375 - 48 = 327px. This is fine.

    **G. Audit all 5 briefing states** (per D-16):
    Walk through the briefing page.tsx render logic and verify each state:
    1. **Morning visit (new articles)**: `newArticles.length > 0` -- renders SectionDivider "New since X" + TopicGroups with isNew + reviewed section. Verify mobile renders correctly.
    2. **Evening visit (mixed)**: Same as morning but with reviewed articles showing. Both sections visible.
    3. **Caught up**: `newArticles.length === 0` -- renders CaughtUpState + reviewed section. Verify copy: "You are caught up."
    4. **Archive**: `isArchiveMode === true` -- renders ArchiveBanner + TopicGroups without feedback. Verify ArchiveBanner copy: "Viewing archive".
    5. **Pending enrichment**: `pendingArticles.length > 0` -- renders PendingSection. Verify "Awaiting enrichment" label.

    For each state, confirm:
    - No text overflow at 375px
    - Touch targets >= 44px for any interactive elements
    - Typography matches design system scale
    - Color tokens used correctly (not hardcoded)
    - Focus-visible styles on all interactive elements

    Document any fixes applied for each state in the summary.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - BriefingCard.tsx contains `pl-4 sm:pl-6` (or similar mobile padding reduction)
    - DateStepper.tsx buttons have min 44px touch target on mobile (contains `min-h-[44px]` or equivalent)
    - StatusBar.tsx uses `flex-wrap` or `flex-col sm:flex-row` for mobile wrapping
    - PendingSection.tsx article titles have overflow protection (`truncate` or `line-clamp`)
    - briefing/page.tsx header container has `flex-wrap` safety
    - All 5 briefing states verified (documented in SUMMARY)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Briefing page renders correctly on mobile for all 5 states. Touch targets adequate, no overflow, typography correct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all views at desktop and mobile</name>
  <files>none</files>
  <action>User visually verifies all views at desktop and mobile viewports. No code changes in this task.</action>
  <what-built>
    Complete UX polish across all views:
    - Feed: fallback visuals (TopicIcon/SourceAvatar), NEW badges, mobile stacked list, responsive toolbar
    - Nav: active link indicator, hamburger menu on mobile
    - Sources: SourceAvatar, inline delete confirmation, mobile responsive
    - Chat: full-width below 640px
    - Briefing: mobile padding, touch targets, all 5 states
    - Typography and dark mode audit applied globally
  </what-built>
  <how-to-verify>
    Start the dev server: `pnpm dev`

    **Desktop (1440px+ viewport):**
    1. Visit http://localhost:3000/ -- Feed page
       - Verify: "Feed" nav link is bold/prominent, others are muted
       - Verify: articles without thumbnails show TopicIcon (colored icon) or SourceAvatar (letter circle)
       - Verify: unread articles show "New" badge if they appeared since your last visit
       - Verify: table columns are properly sized with no empty cells
    2. Visit http://localhost:3000/briefing -- Briefing page
       - Verify: "Briefing" nav link is now bold
       - Verify: cards have left border importance indicator
       - Verify: all sections render (new, reviewed, or caught up state)
    3. Visit http://localhost:3000/sources -- Sources page
       - Verify: "Sources" nav link is bold
       - Verify: each source row has a colored initial circle
       - Verify: clicking delete shows inline "Delete source?" / "Keep source" buttons (no browser dialog)
    4. Toggle dark mode (OS setting or DevTools)
       - Verify: all text readable, cards elevated from background, borders visible

    **Mobile (375px viewport via DevTools):**
    5. Feed page at 375px
       - Verify: table is hidden, stacked list shown
       - Verify: each row has icon/avatar + title + metadata + action buttons
       - Verify: no horizontal scrolling
       - Verify: action buttons are tappable (44px+ height)
    6. Nav at 375px
       - Verify: hamburger icon visible, desktop nav links hidden
       - Verify: tapping hamburger opens drawer from left with Feed/Briefing/Sources links
       - Verify: tapping a link navigates and closes drawer
       - Verify: pressing Escape closes drawer
    7. Briefing at 375px
       - Verify: cards have adequate padding (not cramped)
       - Verify: DateStepper buttons tappable
    8. Sources at 375px
       - Verify: form fields stack vertically
       - Verify: URL column hidden, rest of table fits
    9. Open chat on mobile
       - Verify: chat panel takes full viewport width
       - Verify: no resize handle visible
  </how-to-verify>
  <verify>User approves visual checkpoint</verify>
  <done>All views approved at desktop and mobile. Phase 5 UX Polish complete.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes
- All 5 briefing states verified on mobile
- Visual checkpoint approved by user
</verification>

<success_criteria>
- Briefing renders correctly on mobile at 375px for all states
- All views pass visual inspection at both desktop and mobile widths
- Dark mode audit confirmed
- User approves the visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-04-SUMMARY.md`
</output>
