---
phase: 05-ux-polish
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - src/stories/features/feed/FeedTable.stories.tsx
  - src/stories/features/feed/FeedMobileList.stories.tsx
  - src/stories/features/feed/FeedToolbar.stories.tsx
  - src/stories/features/briefing/BriefingCard.stories.tsx
  - src/stories/features/briefing/TopicGroup.stories.tsx
  - src/stories/features/briefing/DateStepper.stories.tsx
  - src/stories/features/briefing/StatusBar.stories.tsx
  - src/stories/features/briefing/CaughtUpState.stories.tsx
  - src/stories/features/sources/SourceList.stories.tsx
  - src/stories/features/sources/SourceForm.stories.tsx
  - src/stories/features/chat/ChatPanel.stories.tsx
  - src/stories/features/layout/NavLinks.stories.tsx
  - src/stories/features/layout/HamburgerMenu.stories.tsx
  - src/stories/pages/Feed.stories.tsx
  - src/stories/pages/Briefing.stories.tsx
  - src/stories/pages/Sources.stories.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Every feature component has a story covering its key states"
    - "Briefing stories cover all 5 states (morning, evening, caught-up, archive, pending)"
    - "Feed stories show desktop table and mobile stacked list"
    - "Sources stories show default and delete confirmation flow"
    - "Page stories compose feature components with realistic data"
  artifacts:
    - path: "src/stories/features/"
      provides: "Feature component stories organized by domain"
    - path: "src/stories/pages/"
      provides: "Page-level composition stories"
---

# Plan 05-06: Feature Component + Page Stories

<objective>
Create Storybook stories for all feature components and page compositions. Stories use real components with fixture data from fixtures.ts. Cover all meaningful states including the 5 briefing states, feed desktop/mobile, sources delete flow, and chat panel states.
</objective>

## Tasks

<task id="05-06-01" type="auto">
<name>Feed and layout feature stories</name>

<read_first>
- src/components/features/feed/FeedTable.tsx
- src/components/features/feed/FeedMobileList.tsx
- src/components/features/feed/FeedToolbar.tsx
- src/components/features/feed/columns.tsx
- src/components/features/layout/NavLinks.tsx
- src/components/features/layout/HamburgerMenu.tsx
- src/stories/fixtures.ts (fixture data)
</read_first>

<files>
- src/stories/features/feed/FeedTable.stories.tsx
- src/stories/features/feed/FeedMobileList.stories.tsx
- src/stories/features/feed/FeedToolbar.stories.tsx
- src/stories/features/layout/NavLinks.stories.tsx
- src/stories/features/layout/HamburgerMenu.stories.tsx
</files>

<action>
Create stories for each component. Read the actual component to understand its props interface, then write stories that exercise real states.

**FeedTable.stories.tsx**: Stories for:
- Default (with mixed article states from fixtures)
- Empty state (no articles)
- Loading state (if applicable)
- With search query active (to show highlightMatch)
Note: FeedTable is a client component that fetches articles internally. If it can't be rendered in isolation without API calls, mock the fetch calls using Storybook's MSW addon or parameters. Read the component first to understand its data flow.

**FeedMobileList.stories.tsx**: Stories for:
- Default (articles with varied fallback visuals)
- With NEW badges
- With read/unread mix
Note: This component receives articles as props from FeedTable. Should be straightforward to render with fixture data.

**FeedToolbar.stories.tsx**: Stories for:
- Default (all controls)
- With active search
- With category filter selected

**NavLinks.stories.tsx**: Stories for:
- Feed active
- Briefing active
- Sources active
Note: Uses `usePathname()` -- mock via Storybook's Next.js navigation parameter: `parameters: { nextjs: { navigation: { pathname: '/briefing' } } }`

**HamburgerMenu.stories.tsx**: Stories for:
- Closed (default)
- Open (pass isOpen prop or trigger via play function)

IMPORTANT: Read each component's actual props interface before writing the story. Do NOT guess props. If a component fetches data internally (like FeedTable), you may need to mock the API route or provide a decorator. Prefer the simplest approach that shows the real component.
</action>

<verify>
<automated>pnpm build-storybook 2>&1 | tail -5</automated>
</verify>

<done>
- All 5 story files exist in src/stories/features/feed/ and src/stories/features/layout/
- Stories import actual components from @/components/features/
- `pnpm build-storybook` exits 0
</done>
</task>

<task id="05-06-02" type="auto">
<name>Briefing, sources, and chat feature stories</name>

<read_first>
- src/components/features/briefing/BriefingCard.tsx
- src/components/features/briefing/TopicGroup.tsx
- src/components/features/briefing/DateStepper.tsx
- src/components/features/briefing/StatusBar.tsx
- src/components/features/briefing/CaughtUpState.tsx
- src/components/features/sources/SourceList.tsx
- src/components/features/sources/SourceForm.tsx
- src/components/features/chat/ChatPanel.tsx
- src/stories/fixtures.ts (fixture data)
</read_first>

<files>
- src/stories/features/briefing/BriefingCard.stories.tsx
- src/stories/features/briefing/TopicGroup.stories.tsx
- src/stories/features/briefing/DateStepper.stories.tsx
- src/stories/features/briefing/StatusBar.stories.tsx
- src/stories/features/briefing/CaughtUpState.stories.tsx
- src/stories/features/sources/SourceList.stories.tsx
- src/stories/features/sources/SourceForm.stories.tsx
- src/stories/features/chat/ChatPanel.stories.tsx
</files>

<action>
**Briefing stories -- cover all 5 states from D-16:**

**BriefingCard.stories.tsx**:
- Default (with summary, topics, articles)
- With critical importance badge
- Mobile view (narrower container)

**TopicGroup.stories.tsx**:
- With multiple articles
- Single article
- With topic icon displayed

**DateStepper.stories.tsx**:
- Default date
- With navigation (prev/next)
- Mobile touch targets (44px)

**StatusBar.stories.tsx**:
- With stats (article count, topic count)
- Loading/pending state

**CaughtUpState.stories.tsx**:
- Default "You are caught up" state

**Sources stories:**

**SourceList.stories.tsx**:
- Default (list of sources with SourceAvatar)
- Empty state
- Delete confirmation active (showing "Delete source?" / "Keep source" inline)
Note: If SourceList fetches data internally, mock the API. Read the component first.

**SourceForm.stories.tsx**:
- Default (empty form)
- With validation error
- Mobile stacked layout

**Chat stories:**

**ChatPanel.stories.tsx**:
- Default closed
- Open with messages (if applicable)
- Mobile full-width
Note: ChatPanel likely uses AI SDK hooks. Mock as needed. Read the component first to understand data dependencies.

IMPORTANT: For each component, read its source FIRST. Understand what props it takes, what hooks it uses, what data it needs. Then write the simplest story that renders the real component. If a component has complex data dependencies (API calls, hooks), add appropriate mocks or decorators.

Add any new fixture data needed to `src/stories/fixtures.ts` (e.g., briefing data, source list data).
</action>

<verify>
<automated>pnpm build-storybook 2>&1 | tail -5</automated>
</verify>

<done>
- All 8 story files exist in src/stories/features/briefing/, sources/, chat/
- Briefing stories cover all 5 states
- Sources stories show delete confirmation flow
- `pnpm build-storybook` exits 0
</done>
</task>

<task id="05-06-03" type="auto">
<name>Page-level composition stories</name>

<read_first>
- src/app/page.tsx (Feed page)
- src/app/briefing/page.tsx (Briefing page)
- src/app/sources/page.tsx (Sources page)
- src/app/layout.tsx (Root layout with nav)
- src/stories/fixtures.ts
</read_first>

<files>
- src/stories/pages/Feed.stories.tsx
- src/stories/pages/Briefing.stories.tsx
- src/stories/pages/Sources.stories.tsx
</files>

<action>
Create page-level stories that compose the feature components together to show full page layouts.

**These are the tricky ones** because pages are Server Components that fetch data. Two approaches:
1. If RSC experimental support works: import the page component directly, mock fetch/prisma calls
2. If not: compose the client components manually with fixture data to recreate the page layout

Read each page component first. If it's a thin Server Component that passes data to client components, option 2 is cleaner and more reliable.

**Feed.stories.tsx**:
- Default feed view (compose FeedToolbar + FeedTable with fixture articles)
- Empty feed
- With search active

**Briefing.stories.tsx**:
- Morning visit (fresh articles)
- Evening visit (more articles accumulated)
- Caught up (no new articles)
- Archive view (past date)
- Pending enrichment (articles without summaries)

**Sources.stories.tsx**:
- Default (list of sources + add form)
- Empty (no sources added yet)

Each page story should wrap content in the app's layout structure (section-container or reading-container classes) to reflect actual rendering.
</action>

<verify>
<automated>pnpm build-storybook 2>&1 | tail -5</automated>
</verify>

<done>
- All 3 page story files exist in src/stories/pages/
- Briefing page stories cover all 5 states
- `pnpm build-storybook` exits 0
</done>
</task>
