# Plan 04.4-01 Summary: Feed Actions Column + ChatPanel Chip Update

**Commit:** f07bc65
**Files changed:** 4 (+60/-33)

## Accomplishments

- Replaced the standalone "Status" column (Read/Unread text button) with a consolidated **Actions column** as the last column in the feed table
- Actions column contains two icon buttons per row:
  - **Read/Unread toggle**: `CircleCheck` (read, muted) / `Circle` (unread, blue) icons with aria-labels
  - **Chat about this**: `MessageCircle` icon that dispatches `chat-about-this` CustomEvent with article context (id, title, source, publishedAt), same pattern as BriefingCard
- Introduced `ChipConfig` type in `PromptChips.tsx` supporting optional icon components alongside labels
- Replaced "Summarize this article" chip with **"Read this for me"** chip in `CONTEXTUAL_CHIPS`, rendered with a `Sparkles` icon to mark it as an AI-enhanced shortcut
- Updated FeedTable skeleton rows to match the new Actions column width (100px, two icon placeholders)

## Files Modified

- `src/components/features/feed/columns.tsx` -- New actions column definition replacing isRead column
- `src/components/features/feed/FeedTable.tsx` -- Skeleton row updated for new column width
- `src/components/features/chat/ChatPanel.tsx` -- CONTEXTUAL_CHIPS array updated to ChipConfig[], handleChipClick accepts ChipConfig
- `src/components/features/chat/PromptChips.tsx` -- ChipConfig type exported, icon rendering support added
