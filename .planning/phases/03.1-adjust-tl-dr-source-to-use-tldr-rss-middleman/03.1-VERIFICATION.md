---
phase: 03.1-adjust-tl-dr-source-to-use-tldr-rss-middleman
verified: 2026-03-19T23:30:00Z
status: human_needed
score: 5/6 must-haves verified
human_verification:
  - test: "Confirm existing HN articles are preserved under TLDR Tech source after running seed"
    expected: "TLDR Tech source (id=1) retains its article count from before the migration; no articles deleted or orphaned"
    why_human: "Cannot query live DB state programmatically during verification. The seed.ts code uses the correct preservation pattern (URL update in-place), but actual row retention can only be confirmed by inspecting the running database."
---

# Phase 03.1: TLDR Source Correction Verification Report

**Phase Goal:** Replace the mislabeled TLDR Tech source with the correct TLDR Tech feed via tldr-rss middleman, add TLDR AI and TLDR Design sources, and properly name the Hacker News source.
**Verified:** 2026-03-19T23:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | TLDR Tech source URL is bullrich.dev/tldr-rss/tech.rss, not hnrss.org/frontpage | VERIFIED | `prisma/seed.ts` line 6: `url: "https://bullrich.dev/tldr-rss/tech.rss"` in SOURCES array |
| 2 | Hacker News source exists as a separate row with URL hnrss.org/frontpage | VERIFIED | `prisma/seed.ts` line 9: `{ name: "Hacker News", url: "https://hnrss.org/frontpage", category: "Tech" }` |
| 3 | TLDR AI source exists with URL bullrich.dev/tldr-rss/ai.rss and category AI | VERIFIED | `prisma/seed.ts` line 7: `{ name: "TLDR AI", url: "https://bullrich.dev/tldr-rss/ai.rss", category: "AI" }` |
| 4 | TLDR Design source exists with URL bullrich.dev/tldr-rss/design.rss and category Design | VERIFIED | `prisma/seed.ts` line 8: `{ name: "TLDR Design", url: "https://bullrich.dev/tldr-rss/design.rss", category: "Design" }` |
| 5 | Existing articles previously fetched under TLDR Tech are preserved (no deletion, no orphan) | NEEDS HUMAN | Code uses in-place URL update (`prisma.source.update`) with name guard before upsert loop, which is the correct preservation pattern. Cannot confirm DB row state without live query. |
| 6 | Database has exactly 7 sources after seeding (4 original minus TLDR Tech duplication + 3 new) | VERIFIED | SOURCES array contains exactly 7 entries (confirmed by counting `{ name:` occurrences: 7). Upsert-on-url logic ensures idempotency. |

**Score:** 5/6 truths verified (1 needs human confirmation)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `prisma/seed.ts` | Updated SOURCES array with 7 sources and URL-swap preamble | VERIFIED | 46 lines, real implementation. Contains 7-entry SOURCES array, `prisma.source.findUnique` preamble at line 21, `prisma.source.update` at line 25, `staleHnTldr?.name === "TLDR Tech"` guard at line 24, upsert loop at lines 33-38. No stubs, no TODOs, no empty returns. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `prisma/seed.ts` | `prisma/schema.prisma` | PrismaClient Source model upsert | VERIFIED | `import { PrismaClient } from "@prisma/client"` at line 1; calls `prisma.source.findUnique`, `prisma.source.update`, `prisma.source.upsert` — all matching the `Source` model with `url @unique` constraint confirmed in `schema.prisma` line 13. |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SRC-01 | 03.1-01-PLAN.md | Not defined in REQUIREMENTS.md | ORPHANED | ID referenced in ROADMAP.md Phase 03.1 and PLAN frontmatter, but has no entry in `.planning/REQUIREMENTS.md`. Functionally satisfied by the TLDR Tech URL correction (Truth 1). |
| SRC-02 | 03.1-01-PLAN.md | Not defined in REQUIREMENTS.md | ORPHANED | ID referenced in ROADMAP.md and PLAN frontmatter, but has no entry in `.planning/REQUIREMENTS.md`. Functionally satisfied by TLDR AI and TLDR Design additions (Truths 3, 4). |
| SRC-03 | 03.1-01-PLAN.md | Not defined in REQUIREMENTS.md | ORPHANED | ID referenced in ROADMAP.md and PLAN frontmatter, but has no entry in `.planning/REQUIREMENTS.md`. Functionally satisfied by Hacker News source addition (Truth 2). |

**Note:** SRC-01, SRC-02, SRC-03 do not exist in `.planning/REQUIREMENTS.md`. They are referenced only in `ROADMAP.md` and the PLAN frontmatter. The underlying functionality they imply is implemented correctly in `prisma/seed.ts`, but the requirements traceability table in REQUIREMENTS.md has a gap: these IDs and their descriptions are absent.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/features/briefing/BriefingCard.tsx` | 55 | `react-hooks/static-components` — Icon component created during render | Warning | Pre-existing issue, explicitly noted in SUMMARY.md as out of scope for this phase. Does not affect seed or source data. |

No anti-patterns found in `prisma/seed.ts` (the sole modified file for this phase).

---

### Human Verification Required

#### 1. Article relation preservation after seeding

**Test:** Connect to the dev database and run:
```sql
SELECT s.id, s.name, s.url, COUNT(a.id) as article_count
FROM "Source" s
LEFT JOIN "Article" a ON a."sourceId" = s.id
WHERE s.url = 'https://bullrich.dev/tldr-rss/tech.rss'
GROUP BY s.id;
```
Or via Prisma:
```bash
DATABASE_URL="file:./prisma/dev.db" npx tsx -e "
  const { PrismaClient } = require('@prisma/client');
  const p = new PrismaClient();
  p.source.findUnique({ where: { url: 'https://bullrich.dev/tldr-rss/tech.rss' }, include: { _count: { select: { articles: true } } } })
    .then(s => console.log('TLDR Tech id:', s?.id, 'articles:', s?._count?.articles))
    .finally(() => p.\$disconnect());
"
```
**Expected:** TLDR Tech source has `id=1` (original row retained) and an article count greater than 0 (HN articles preserved under this source).
**Why human:** Live database state cannot be inspected programmatically during static verification. The code is correct, but runtime confirmation is needed once.

---

### Gaps Summary

No functional gaps detected. The single must-have requiring human verification (article relation preservation) is a runtime DB state check, not a code deficiency. The seed.ts implementation uses the correct in-place URL update pattern that preserves FK relations.

The requirements SRC-01, SRC-02, SRC-03 are ORPHANED from REQUIREMENTS.md. They are defined and implemented at the code level but are not formally documented in `.planning/REQUIREMENTS.md`. This is a documentation inconsistency: consider adding SRC-01/02/03 entries to REQUIREMENTS.md and the traceability table to close the gap.

---

_Verified: 2026-03-19T23:30:00Z_
_Verifier: Claude (gsd-verifier)_
